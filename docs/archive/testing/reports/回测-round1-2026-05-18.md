# 回测 Round 1 测试报告

- **日期**：2026-05-18
- **测试人**：AI Agent（chrome-devtools MCP）
- **代码版本**：当前 main 分支
- **环境**：dev（`http://localhost:3039`），账号 `superadmin`
- **覆盖模块**：回测 9 个路由（工作台 / 任务历史 / 任务详情 / Walk-Forward × 3 / 多策略对比 × 3）

---

## 1. 测试矩阵

| #   | 页面              | 路径                              | 结果       | 备注                                            |
| --- | ----------------- | --------------------------------- | ---------- | ----------------------------------------------- |
| 1   | 回测工作台        | `/backtest`                       | ✅ Pass    | 5 个策略模板、基础配置、交易执行、提交摘要齐全  |
| 2   | 任务历史          | `/backtest/runs`                  | ✅ Pass    | 已有 3 条 run（QA-MA-Test-001 / 策略1 / 策略2） |
| 2.1 | 任务详情          | `/backtest/runs/:runId`           | ✅ Pass    | KPI 12 项 + 净值/回撤/月热力 全渲染             |
| 2.2 | 详情·交易明细     | tab                               | ✅ Pass    | 空态优雅                                        |
| 2.3 | 详情·持仓快照     | tab                               | ✅ Pass    | 日期选择 + 空态优雅                             |
| 2.4 | 详情·调仓日志     | tab                               | ✅ Pass    | 空态优雅                                        |
| 2.5 | 详情·运行配置     | tab                               | ✅ Pass    | 基础配置 + 策略参数完整渲染                     |
| 2.6 | 详情·高级分析     | tab（4 子 tab）                   | ✅ Pass    | 蒙特卡洛/归因/成本敏感性/参数扫描渲染           |
| 3   | Walk-Forward 列表 | `/backtest/walk-forward`          | ✅ Pass    | 空态优雅；KPI/筛选/排序齐全                     |
| 3.1 | Walk-Forward 创建 | `/backtest/walk-forward/create`   | ✅ Pass    | 滚动 WF 默认生成 36 窗口预览                    |
| 3.2 | Walk-Forward 详情 | `/backtest/walk-forward/:wfRunId` | ⏭️ Skipped | 无任务数据，留待 Round 2                        |
| 4   | 多策略对比 · 历史 | `/backtest/comparison`            | ✅ Pass    | 1 条历史任务（策略1 vs 策略2）渲染正常          |
| 4.1 | 多策略对比 · 详情 | `/backtest/comparison/:groupId`   | ✅ Pass    | 净值对比折线 + 指标对比表（最优值高亮）齐全     |
| 4.2 | 多策略对比 · 新建 | `/backtest/comparison/create`     | ✅ Pass    | 策略1/策略2 默认填充、通用参数表单渲染          |

控制台仅 1 条 WebSocket warn（benign）+ 2 条 form label issue（已存在，跨模块共性，非本轮新增）。

---

## 2. 修复清单

本轮**无代码修复**：所有 9 个路由首次烟测全部 Pass，未发现 P0/P1 崩溃或 BE 错误外漏。

---

## 3. Observations（未修复）

- **OBS-BT-A · P3**：表单可访问性 — 控制台报 7 个「No label associated with a form field」+ 7 个「form field should have id/name」。涉及范围跨模块（工作台 / WF 创建 / 对比创建均出现）。
- **OBS-BT-B · P3**：`/backtest/runs` KPI 卡「平均耗时」固定显示 `0s`，3 条已完成任务的耗时分别为 `0s/0s/1s`，疑似 BE `stats` 字段未对接，FE 当前以页内聚合兜底（提示文案「聚合统计待后端 stats」与现象一致）。

详见 `docs/testing/已知问题待办.md`。

---

## 4. 验证记录

- 浏览器逐路由烟测（含 5 个 tab 切换、2 个对比详情指标对比、WF 创建窗口预览）均无 JS 错误。
- 无代码改动，跳过 eslint + build（上一轮策略管理已通过：`✓ built in 7.42s`）。

---

## 5. Round 2 计划

1. 在 BE 接通后真正提交一次回测（短区间均线择时），覆盖 `/backtest` 工作台「提交 → 跑批进度 → 详情各 tab 真实数据」全链路。
2. 真正创建一个 Walk-Forward 任务，回归 `/backtest/walk-forward/:wfRunId` 详情页。
3. 真正提交一次多策略对比，回归净值曲线、指标对比表的最优值高亮与失败任务取消流程。
4. OBS-BT-A：核查 form 控件 id/name + label 绑定（可能批量影响 a11y）。
5. OBS-BT-B：BE 接通 `stats` 后回归任务历史 KPI 区。
