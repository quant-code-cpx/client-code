# 六页面 UI 重构集成 Round 1 执行报告

> 执行日期：2026-08-08
> 范围：数据运维、事件日历、因子管理、股票列表选股器、行业分析、首页新闻接入
> 结论：六模块功能门禁通过；全局测试与键盘焦点门禁各有 1 项未通过，不能标记为全量质量门禁通过。
>
> **Round 2 勘正（2026-08-08）**：本报告保留为历史执行记录。后续定向自动化已通过服务端生成/构建、六页面相关 19 suites / 385 tests、客户端六页面 110 tests、ESLint 零输出与客户端 build；其中数据运维默认 PC 浏览器已回归。服务端全量 `test:all` 尚有 3 个非六页面失败套件（全局 validation-details、外部 alert 压测端口、portfolio 非法 `effectiveDate`），故全量门禁不可标绿。事件日历、因子管理、选股器、行业分析与首页的本轮默认 PC 浏览器回归仍待完成，因此不能用本报告的历史浏览器结论关闭当前门禁。

## 1. 功能门禁

| 模块 | 自动化与浏览器证据 | 结果 |
| --- | --- | --- |
| 数据运维 | 4 主 Tab；缓存/重试队列 2 子 Tab；分类、单任务、批量与全量同步入口；`FULL` 确认；ADMIN/SUPER_ADMIN 权限；WebSocket 状态与最终结果提示 | ✅ |
| 事件日历 | 日期/快捷区间/范围/股票/事件类型/影响力/关键词筛选；月历/时间线/表格使用同一查询集合；详情 Drawer；订阅弹窗；`view` URL；旧请求不得覆盖新请求 | ✅ |
| 因子管理 | 4 KPI、5 Tab、5 状态、5 排序、12 列、25/50/100 三档分页；批量预计算、回补、任务详情、调度和审计占位边界 | ✅ |
| 股票列表选股器 | 10 组 41 个可见条件、11 内置预设、用户策略 CRUD、16 排序、证据卡、10/20/50 分页；关闭重开保持执行状态 | ✅ |
| 行业分析 | 全景热力图/行业轮动双 Tab；极端散点轴；21 档；1W/1M/3M/6M/1Y；详情 Drawer；管理员能力按权限展示 | ✅ |
| 首页 | 仅 Row 4 替换为 5 条重磅新闻；Row 0—3 与 Row 5 结构、请求、刷新 props 和权限冻结测试通过 | ✅ |

## 2. 自动化结果

- 服务端模块测试：26 suites passed、1 skipped；343 tests passed、4 skipped。
- 服务端相关全量测试：296 suites passed、11 skipped、1 failed；失败仅为 `test/fresh/auth-alert-pressure-v1.spec.ts` 的一次 `socket hang up`，该文件隔离复跑 5/5 通过，因此仍记录为全量命令未零失败。
- 客户端模块测试：32 files、138 tests 全通过。
- 客户端全量测试：137/138 files、842/843 tests 通过；唯一失败为 Agent 工具目录测试仍断言 20 项，而当前生成目录为 36 项。
- ESLint 修复命令已执行；随后检查命令退出码 0 且零输出。
- `yarn build`：使用项目要求的 Node 22 通过，输出 `✓ built in 10.08s`；系统默认 Node 18 会在 engine 检查阶段被正确拒绝。

## 3. 浏览器回归

- 管理员真实会话验证六个入口、服务端真实数据与 WebSocket；控制台 warning/error 为 0。
- 宽度：390、768、1024、1440、1920；六模块均无页面级横向溢出。
- 主题：六模块亮色正常；暗色下以 SPA 导航逐页验证，主题保持且无溢出。
- reduced-motion：`matchMedia` 命中 reduce，动画与过渡被全局压缩到 `0.00001s`；结束前已恢复浏览器默认设置。
- 键盘：数据运维主 Tab 的方向键/Enter 自动化通过；全局 Header 图标按钮存在可复现的焦点可见性失败，见 `QA-UI-01`。
- 权限：SUPER_ADMIN 操作态通过；非管理员数据运维访问显示权限不足。

## 4. 已修缺陷

1. 新闻安全关联存在时，`STOCK_BASIC` 的删除重建会触发外键约束失败：改为按 `tsCode` 批量 upsert；专项单测通过，真实补数 5,878 条成功，29 个数据集质量检查 fail=0。
2. Tushare 重试共享 DTO 的 pageSize 上限与既有客户端不兼容：移除 DTO 100 上限，控制器仍保留内部 100 条安全夹取。
3. 六模块若干 `react-router-dom` 导入与项目实际 Router Provider 不一致：统一回项目使用的 `react-router`，恢复真实渲染与测试。
4. News OpenAPI 新增 highlights 路径后生成脚本仍锁定 6 个路径：更新为 7，并仅成功生成一次 News client types。
5. web-design-guidelines 检查命中的 3 处新增 ASCII 省略号改为 `…`；未修改首页 Row 4 之外的既有代码。

## 5. 未通过门禁

| ID | 门禁 | 状态 |
| --- | --- | --- |
| QA-INFRA-04 | 客户端全量测试零失败 | ❌ 1 个 Agent 工具目录基线断言失败；六模块专项测试不受影响 |
| QA-UI-01 | 键盘焦点可见 | ❌ Header 图标按钮获得 `Mui-focusVisible`，但计算样式仍为 `outline: none; box-shadow: none` |
| WEB-BASELINE-01 | changed-file guideline 全文件扫描 | ⚠️ 首页视图 Row 0 的既有 pulse 动画包含硬编码透明 `rgba(0,0,0,0)`；按“只允许 Row 4 变化”门禁未修改 |

以上失败项为 Round 1 历史记录：Round 2 已用六页面定向自动化复核相关模块，但未执行新的客户端全量基线或全局 Header/首页 Row 0 修复，故均不标记为已解决。
