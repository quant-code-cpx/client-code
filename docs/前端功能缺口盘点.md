# 前端功能缺口盘点（基于后端 243 端点）

> **盘点日期**：2026-07-20（局部更新：智能体富响应块）
> **后端总端点**：260+ 个 · 22 个 Controller
> **前端覆盖率**：**260+/260+（100%）** 🎉

---

## 变更日志（2026-07-20）

| 模块     | 变更说明                                                                                                                                       |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 预警监控 | 涨跌停明细「次日表现」已接入 `POST /api/alert/limit-next-day-perf`；前端按后端 `items + nextPctChg` 聚合为「昨日板高 × 次日表现」矩阵。 |
| Agent | Batch 017 已完成安全 Markdown、六类白名单内容块、Tool 脱敏摘要、引用/provenance、表格/图表/K线/财务/风险展示和移动端 E2E；真实模型、Tool、BullMQ 与金融口径闭环留给 Batch 018。 |

---

## 后端缺口（股票详情 v3 · 2026-05-04）

> 前端已完成 v3 首批范围收敛：主 Tab 改为行情 / 分析 / 财务 / 公司与股本，「我的研究」改为 NotesDrawer，行情图只保留日 / 周 / 月并移除 `fetchMinuteKline()` 调用路径。以下能力待后端补齐后可继续启用 v3 深度体验。

| 编号    | 后端能力                                                                                                        | 前端落点                                        |
| ------- | --------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| BE-SD-1 | `chart()` 响应附 `events: Array<{ tradeDate, type, label, severity? }>`，覆盖财报、分红、停复牌、解禁等事件     | K 线事件 marker 与事件归因 tooltip              |
| BE-SD-2 | 新增或扩展 peers 接口，返回 `industryPercentile / historicalPercentile5y / historicalPercentile10y / peerGroup` | 分析 Tab 对标分位与同行样本说明                 |
| BE-SD-3 | `overview.relatedAssets` 返回当前用户下 watchlists / alerts / reports / notes 关联资产                          | 头部「已加入 X / X 条预警 / 最近笔记」反馈 chip |
| BE-SD-4 | `overview.todayHeadline` 与 `dataLineage`，并明确 `quoteStatus/latestTradeDate/capabilities` 枚举               | 今日定调、数据审计 tooltip、能力显隐与降级      |
| BE-SD-5 | 新增 `POST /api/stock/detail/analysis/chip-distribution-timeline` 或在现有筹码接口扩展 `timeline`               | 筹码历史回放滑块                                |
| BE-SD-6 | 研究笔记 create/update 支持保存 `snapshotPrice / snapshotTradeDate / snapshotPctChg / snapshotName`             | 从股票详情新建笔记后可回看当时价格快照          |
| BE-SD-7 | 自选股归属检查与多分组返回（如 `watchlist/stocks/check` 支持 `tsCode` 批量返回所在分组）                        | 详情页「加入自选」SplitButton 与已在分组状态    |

> 本期明确**不需要**分钟级 K 线、WebSocket/SSE 实时行情、30 秒轮询行情；这些能力不进入股票详情 v3 当前后端排期。

---

## 后端缺口（数据同步重构 · 2026-05-02）

> 前端已完成数据同步 v2：独立「数据运维」导航、ADMIN 只读 / SUPER_ADMIN 可操作、固定总览 + 4 Tab、保鲜度热条、当日时间线、数据质量 Accordion、日志 payload Drawer、缓存/重试运维工具合并。以下能力待后端补齐后可从禁用态或前端估算升级为完整闭环。

| 编号    | 后端能力                                                                                                               | 前端落点                                       |
| ------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| BE-DS-1 | 新增 `POST /api/tushare/admin/data-freshness`，返回分类/数据集最新 `trade_date`、入库时间、lagDays、是否交易日正常延迟 | 总览保鲜度热条从前端估算升级为精确数据保鲜度   |
| BE-DS-2 | 新增 `POST /api/tushare/admin/sync/cancel`，body `{ jobId, reason }`，并推送取消结果                                   | 运行中同步任务取消入口                         |
| BE-DS-3 | 新增 `POST /api/tushare/admin/retry-queue/items/retry` 与 `items/delete`，支持 `{ ids, reason }`                       | 重试队列单条/批量重试、删除按钮正式启用        |
| BE-DS-4 | 新增 `POST /api/tushare/admin/cache/clear`，支持按 namespace 或全量清理，并返回影响 key 数                             | 缓存统计行内清理与清除全部缓存按钮正式启用     |
| BE-DS-5 | 新增调度暂停/更新端点，支持 cron、enabled、优先级、限流等字段                                                          | 同步计划 cron 查看/编辑与暂停任务              |
| BE-DS-6 | 新增 `POST /api/tushare/admin/sync/performance`，返回任务耗时 P50/P95、异常倍数、历史趋势                              | 当日时间线增加耗时基线告警与异常高亮           |
| BE-DS-7 | 新增 `POST /api/tushare/admin/audit-log` 或复用审计模型并支持 `category='tushare-sync'`                                | 操作审计抽屉接入真实操作流水                   |
| BE-DS-8 | 新增告警订阅端点与 WebSocket payload 增强：jobId、durationMs、rowCount、affectedDatasets、errorStack                   | 告警订阅按钮、失败影响面说明与实时精确局部更新 |

详见 `docs/design/数据同步-后端提示词.md` 与 `docs/design/数据同步-重构-前端设计.md` 第七章。

---

## 后端缺口（组合重构 · 2026-05-02）

> 前端已完成组合 v1 首批兼容改造：列表增强卡、类型筛选/搜索/排序、详情 6 卡摘要、今日盈亏并发兜底、持仓快捷动作、调仓高级参数与委托清单复制。以下能力待后端补齐后可直接启用完整体验。

| 编号    | 后端能力                                                                                                                                                   | 前端落点                                                       |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| BE-PF-1 | `POST /api/portfolio/list` 返回 `kind/isArchived/todayPnl/todayPnlPct/totalMarketValue/cumulativeReturn/lastUpdated/isTradingDay/sparkline`                | 列表增强卡从兼容兜底升级为真实今日盈亏、累计收益与净值迷你曲线 |
| BE-PF-2 | `POST /api/portfolio/detail` 的 `summary` 增加 `todayPnl/todayPnlPct/cumulativeReturn/lastUpdated/isTradingDay`；或确保 `pnl/today` 返回 nullable 今日字段 | 详情页 6 卡摘要与非交易日提示                                  |
| BE-PF-3 | 新增 `POST /api/portfolio/risk/snapshot` 聚合行业/集中度/市值/Beta                                                                                         | 风险 Tab 请求数从 4 个降到 1 个，并统一显示新鲜度              |
| BE-PF-4 | 新增 `POST /api/portfolio/cash/adjust`，支持入金/出金/股息/费用/税费/拆股/配股/成本调整并写审计                                                            | 现金账户面板与公司行动手动兜底                                 |
| BE-PF-5 | 新增 `POST /api/portfolio/holding/bulk-import`，支持 MERGE/REPLACE 与行级错误                                                                              | 持仓 CSV 批量导入                                              |
| BE-PF-6 | 新增 `duplicate/archive/restore` 组合生命周期端点                                                                                                          | 列表卡复制/归档/恢复菜单启用                                   |
| BE-PF-7 | 新增 `trade-log/manual/update/delete`，仅允许手工条目编辑删除                                                                                              | 交易日志抽屉手动记账与复盘修正                                 |
| BE-PF-8 | 新增 `POST /api/portfolio/overview` 和 `rebalance/templates/*`                                                                                             | 总览 Tab 与调仓模板复用                                        |
| BE-PF-9 | 后续新增 `rebalance/execute`（仅 PAPER/手工账）、多基准 performance、公司行动自动处理、WebSocket PnL 推送                                                  | 模拟盘执行、组合业绩高级对比与盘中刷新                         |

详见 `docs/design/组合重构-后端支持提示词.md` 与 `docs/design/组合-重构-前端设计.md` 第五章。

---

## 后端缺口（用户管理重构 · 2026-05-02）

> 前端已完成用户管理 v2 重构：KPI 摘要、URL 状态、排序、批量操作条、强密码生成、配额不限控件、角色调整/解锁/恢复渐进入口、审计 diff 展示与请求竞态治理。以下能力待后端补齐后可打开对应 feature flag。

| 编号     | 后端能力                                                                                             | 前端落点                                           |
| -------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| BE-UM-1  | 新增 `POST /api/user/stats`，返回 `{ total, todayNew, active30d, deactivated, locked }`              | 用户管理顶部 KPI 摘要从 mock/降级切换为真实统计    |
| BE-UM-2  | 新增 `POST /api/user/search`，按账号/昵称模糊搜索                                                    | 审计日志操作者/目标用户 Autocomplete               |
| BE-UM-3  | 新增 `POST /api/user/update-role`，body `{ id, role, reason }`，写 `USER_UPDATE_ROLE` 审计           | 角色调整弹窗正式启用                               |
| BE-UM-4  | 新增 `POST /api/user/unlock` 与列表字段 `lockedUntil/loginFailCount`                                 | 锁定态展示与解锁操作                               |
| BE-UM-5  | `POST /api/user/list` 支持 `includeDeleted/createdFrom/createdTo`，并返回 `lastLoginAt/updatedAt`    | 包含已删除、注册时间筛选、最近登录列与服务端排序   |
| BE-UM-6  | 新增 `POST /api/user/restore` 与软删除语义保持业务资产不级联删除                                     | DELETED 行恢复用户                                 |
| BE-UM-7  | 新增 `POST /api/user/delete-impact`                                                                  | 删除确认弹窗显示自选股/策略/回测/订阅/预警影响清单 |
| BE-UM-8  | 审计 action 扩展登录/登出/Token/解锁/恢复/批量操作，并返回 `result/details.changes/reason/userAgent` | 审计日志结果列、登录类安全事件与字段级 diff        |
| BE-UM-10 | 新增 `bulk-update-status`、`bulk-update-quota`，返回 `success[]/failed[]`                            | 批量操作从前端循环升级为后端批处理                 |

详见 `docs/design/用户管理-重构-前端设计.md` 第七章。

---

## 后端缺口（回测工作台重构 · 2026-05-02）

> 前端已完成工作台 v2 交互与优雅降级；以下字段/端点由后端补齐后可直接启用更精确的估算、字段级错误和跨端自动保存。

| 编号    | 后端能力                                                                                                                                                                                                         | 前端落点                                                          |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| BE-BT-1 | `POST /api/backtests/strategy-templates` 响应增加 `defaultConfig`、`recommendedRange`、`tags`、`difficulty`                                                                                                      | 模板切换时以后端默认参数重置策略配置、模板卡标签与难度展示        |
| BE-BT-2 | `POST /api/backtests/runs/validate` 响应增加 `fieldErrors[]`、`estimatedRebalanceCount`、`estimatedTradeCount`、`estimatedRuntimeSeconds`、`dataGapPercentage`、`recommendedBenchmark`、`similarCompletedRuns[]` | 字段级错误、估算卡、推荐基准、相似配置提示                        |
| BE-BT-3 | `POST /api/backtests/runs` 确认透传 `enableT1Restriction`、`partialFillEnabled`，默认建议 `true / true`                                                                                                          | 交易执行开关与任务创建请求                                        |
| BE-BT-4 | 新增 `POST /api/strategy-draft/auto-save` 与 `POST /api/strategy-draft/auto-save/get`                                                                                                                            | 「上次编辑」自动保存；当前前端已用 localStorage 按用户兜底        |
| BE-BT-5 | 新增 `POST /api/trade-cal/latest` 返回 `latestTradeDate`、`earliestTradeDate`（`YYYYMMDD`）                                                                                                                      | 首屏默认日期与 DatePicker min/max，无需等待首次 validate          |
| BE-BT-6 | `POST /api/backtests/runs/list` 支持 `statuses: string[]`                                                                                                                                                        | 运行中任务徽标可单次请求拉取 QUEUED/RUNNING；当前前端两次请求合并 |

详见 `docs/design/回测工作台-重构-前端设计.md` 第七章。

---

## 后端缺口（回测任务历史重构 · 2026-05-02）

> 前端已完成 `/backtest/runs` v2 兼容版：URL 筛选、DatePicker 区间、KPI 兜底聚合、WebSocket 进度、复制重跑完整预填、批量加入对比已可用。以下能力待后端补齐后，前端已预埋入口可直接启用。

| 编号     | 后端能力                                                                                                                                                                          | 前端落点                                                                           |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| BE-BRH-1 | `POST /api/backtests/runs/list` 入参支持 `createdStart/createdEnd/sort/order/archived/tagIds/creatorId/mineOnly/failedReasonCode`，参数全部走 Body                                | 日期范围、服务端排序、归档筛选、标签筛选、我的任务、失败原因筛选                   |
| BE-BRH-2 | `runs/list` 返回补充 `jobId/startedAt/durationSeconds/failedReasonCode/failedReasonLabel/strategyConfigSummary/creatorName/source/starred/archived/tags/queuePosition/etaSeconds` | WebSocket 订阅免 detail 兜底、耗时列、失败摘要、创建人/来源、标签/收藏、队列可见性 |
| BE-BRH-3 | 新增 `POST /api/backtests/runs/stats`，按列表同一筛选口径返回总量、完成率、平均耗时、失败原因 Top 3                                                                               | 顶部 KPI 摘要从“当前页兜底”升级为全量统计                                          |
| BE-BRH-4 | 新增 `rename/delete/archive/star/retry` 操作端点；删除建议软删并保留审计                                                                                                          | 行内菜单与批量操作启用                                                             |
| BE-BRH-6 | 新增 tags 体系：标签 CRUD、单 run 设置标签、批量追加/移除标签、列表按 `tagIds` 筛选                                                                                               | 标签筛选、行内标签、批量打标签                                                     |
| BE-BRH-7 | WebSocket payload 增强：`backtest_progress/completed/failed` 增加 `runId/status/progress/summary/failedReasonCode`                                                                | 列表局部 patch 更精确，减少 completed/failed 后整页刷新                            |

详见 `docs/design/回测任务历史-后端提示词.md` 与 `docs/design/回测任务历史-重构-前端设计.md` 第五章。

---

## 后端缺口（多策略对比重构 · 2026-05-02）

> 前端已完成第一批实现：新增 `/backtest/comparison` 历史列表页、列表卡片、分页/状态/关键词筛选、取消/删除/复制配置入口，并扩展 `src/api/backtest.ts` 的多策略对比增强端点类型。以下能力待后端补齐后，列表与后续高级 Tab 可直接联调启用。

| 编号    | 后端能力                                                                                                                      | 前端落点                                                  |
| ------- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| BE-BC-1 | `POST /api/backtests/comparisons/list` 支持 `page/pageSize/status/keyword/startDate/endDate/strategyType`，返回 `items,total` | `/backtest/comparison` 历史列表、KPI 兜底聚合、分页与筛选 |
| BE-BC-2 | `POST /api/backtests/comparisons/config` 返回 common/cost/constraint/strategies 完整配置                                      | 历史列表「复制配置重跑」→ 创建页预填；详情页配置回看      |
| BE-BC-3 | `ComparisonGroupDetail` 增加 `failures/etaSeconds/progress/strategyCount/completedCount` 并支持 `PARTIAL`                     | 详情页部分失败提示、刷新后 jobId 恢复、后续运行进度卡     |
| BE-BC-4 | `CreateComparisonQuery` 支持成本、持仓约束、撮合价与策略级 override                                                           | 创建页成本与约束面板（下一批实现）                        |
| BE-BC-5 | `POST /api/backtests/comparisons/rolling` / `monthly` / `correlation` / `envelope`                                            | 详情页滚动统计、月度矩阵、后端计算相关性、等权包络线 Tab  |
| BE-BC-6 | `POST /api/backtests/comparisons/cancel` / `delete` / `append`                                                                | 列表任务管理、详情页增量加策略；删除建议软删              |

详见 `docs/design/多策略对比-重构-前端设计.md` 第五章与 5.5 后端实现提示词。

---

## 后端缺口（Walk-Forward 验证重构 · 2026-05-02）

> 前端已完成 `/backtest/walk-forward/*` v2 三屏重构，并按渐进增强预埋未来端点；以下能力由后端补齐后，稳健性诊断与窗口钻取会从前端兜底展示升级为完整明细。

| 编号    | 后端能力                                                                                                                             | 前端落点                                           |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------- |
| BE-WF-1 | `POST /api/backtests/walk-forward/runs/list` 入参支持 `q/statuses/strategyTypes/sortBy/sortDir/page/pageSize`，响应增加 `aggregates` | 列表 URL 筛选、排序、分页与 KPI 全量聚合           |
| BE-WF-2 | `walk-forward/runs` 创建入参支持 `windowMode/purgeDays/embargoDays/minOosTrades`；返回队列、窗口数量与阶段状态                       | 创建页模式 Tabs、防泄漏字段、窗口预算与进度卡      |
| BE-WF-3 | `walk-forward/runs/detail` 响应增加 `baseStrategyConfig/paramSearchSpace/windowMode/wfe/robustnessLevel/oosNegativeWindowRate`       | 详情配置复盘、WFE 卡片、综合稳健性标签             |
| BE-WF-4 | `walk-forward/runs/equity` 增加 `benchmarkNav` 与窗口边界元数据                                                                      | OOS 净值与基准对比、后续窗口边界标注               |
| BE-WF-5 | 新增 `cancel/delete/clone/save-as-strategy` 等操作端点，全部 POST Body                                                               | 列表/详情操作入口启用；当前失败时提示待后端支持    |
| BE-WF-6 | 新增窗口钻取端点：`window-detail/window-trades/window-positions/window-rebalance-logs`                                               | 窗口抽屉展示成交明细、持仓快照、调仓日志与窗口净值 |
| BE-WF-7 | WebSocket `backtest_progress` 增加 `stage/windowIndex/completedWindows/windowCount/etaSeconds`                                       | 详情运行进度卡展示阶段、窗口完成数与 ETA           |
| BE-WF-8 | Rolling 与 WF 返回统一 `wfRunId/jobId/status` 契约，并支持列表/详情统一查询                                                          | 创建页 Rolling Tab 与 WF 列表/详情共用工作流       |

详见 `docs/design/Walk-Forward验证-重构-前端设计.md` 第五章与 5.5 后端提示词。

---

## 后端缺口（信号历史重构 · 2026-05-02）

> 前端已完成历史复盘与多策略对比 UI，并通过 mock 数据演示完整链路；以下能力由后端补齐后可直接替换 mock/静态兜底。

| 编号    | 后端能力                                                                                                                                                               | 前端落点                            |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| BE-SH-1 | `POST /api/signal/history` 出参增加 `aggregateStats`、`groups[].generatedAt`、`groups[].diffFromPrev`、`signals[].forwardReturn/excessReturn/isFirstOccurrence/reason` | 摘要卡、分组 diff、收益列、因子贡献 |
| BE-SH-2 | `POST /api/signal/history` 入参支持 `actions[]`、`stockKeyword`、`confidenceMin/Max`、`forwardWindow`、`viewMode`、`showHold`、分页字段                                | 服务端筛选与 URL 状态               |
| BE-SH-3 | 新端点 `POST /api/signal/history/detail`：按 `{ strategyId, tradeDate }` 返回当日全量信号与生成元信息                                                                  | 单日详情抽屉                        |
| BE-SH-4 | 新端点 `POST /api/signal/history/compare`：按最多 3 个策略返回区间聚合指标                                                                                             | 多策略历史对比页                    |

详见 `docs/design/信号历史-重构-前端设计.md` 第五章。

---

## 后端缺口（因子高级分析重构 · 2026-XX）

> 前端已按"优雅降级"策略上线，相关 UI 控件以 Tooltip 标注「后端能力升级中」并 disable，待后端补齐字段即可零改动启用。

| 编号 | 后端能力                                                                                                                                                          | 前端落点                                         |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| BE-1 | `/api/factor/analysis/orthogonalize` 响应增加 `residualVarianceRatio: number[]`（每个因子被解释掉的方差占比）                                                     | 正交化结果"残差解释力"柱状图                     |
| BE-2 | `/api/factor/analysis/fama-macbeth` 请求支持 `neweyWestLag?: number`，响应补 `tStatNW` / `pValueNW` / `seriesPerDate[]`（每期 lambda 序列，用于热度图与拒绝率图） | NW 调整开关 + 时序面板                           |
| BE-3 | `/api/factor/optimization` 请求支持 `benchmarkCode`，响应补 `sectorExposure[]` / `riskContribution[]` / `benchmarkComparison{alpha,beta,trackingError}`           | 行业暴露条形图 / 边际风险贡献列 / Benchmark 摘要 |
| BE-4 | 协方差估计方式：请求增加 `covMethod?: 'sample' \| 'ledoit_wolf' \| 'ewma'`，响应回写 `covMethodUsed`                                                              | 优化面板"协方差估计"下拉                         |
| BE-5 | 正交化方法增加 `'gram-schmidt'`（按选择顺序逐个剥离）                                                                                                             | 正交化方法第三个 Radio                           |
| BE-6 | 运行历史持久化：`/api/factor/analysis/runs`（列表 / 详情 / 删除），账号级；当前前端用 localStorage 兜底                                                           | 历史抽屉                                         |
| BE-7 | "最近交易日"端点：`/api/market/calendar/last-trading-day`（解决周末/节假日选默认日期问题）                                                                        | 日期选择器默认值                                 |

详见 `docs/design/因子高级分析-重构-前端设计.md` 附录 A。

---

## 变更日志（2026-05-01 → 2026-05-02）

### 前端变更（数据同步重构）

| 模块     | 变更说明                                                                                                                                                                          |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 数据同步 | 用户决策落地：后端缺口以 `docs/design/数据同步-后端提示词.md` 输出；数据同步独立为一级「数据运维」菜单；ADMIN 只读、SUPER_ADMIN 可操作                                            |
| 数据同步 | 路由调整为 `/tushare-sync` 主入口，`/admin/tushare-sync` 兼容重定向；页面顶部展示角色权限、实时连接状态、刷新、告警订阅占位与审计抽屉入口                                         |
| 数据同步 | 页面信息架构从 5 Tab 收敛为固定总览 + 任务调度 / 同步日志 / 数据质量 / 运维工具 4 Tab，缓存统计与重试队列合并为运维工具内部 SubTab                                                |
| 数据同步 | 状态总览增强：分类保鲜度热条、当日同步时间线、下钻日志/质量入口；任务调度增加最后状态列、分类快捷同步、单任务立即同步和全量同步 `FULL` 二次确认                                   |
| 数据同步 | 同步日志增强：今天/失败/近 7 天快捷筛选、payload Drawer；数据质量改为三段 Accordion，数据缺口数据集选择升级为分组 Autocomplete                                                    |
| 数据同步 | 缓存与重试队列增加搜索、多选和后端待启用操作占位；`src/api/tushare-sync.ts` 预埋 freshness/cache clear/retry items/cancel/performance 等 v2 API；通过 ESLint 与 `yarn build` 验证 |

### 前端变更（组合重构 · 第一批）

| 模块 | 变更说明                                                                                                                                                                       |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 组合 | 将待确认 Q1-Q6 改为推荐决策：默认支持 PAPER/LIVE、实盘不自动执行调仓、先单基准可切换、列表轻量对比优先、引入归档/复制、公司行动后端自动为主前端兜底                            |
| 组合 | 新增 `docs/design/组合重构-后端支持提示词.md`，沉淀 list/detail/risk snapshot/cash adjust/bulk import/duplicate/archive/manual trade-log/overview/rebalance execute 等后端契约 |
| 组合 | 列表页增强：类型筛选、搜索、排序、增强卡展示类型/更新时间/今日盈亏/今日涨幅/总市值/累计收益/持仓数，后端新字段缺失时向后兼容                                                   |
| 组合 | 详情页摘要扩展为 6 卡，并并发拉取 `pnl/today`；持仓/组合写操作后通过 `refreshKey` 重新挂载已访问 Tab，降低跨 Tab 脏读                                                          |
| 组合 | 调仓对话框增强：股票 Autocomplete、从当前持仓带入、佣金率/最小佣金/印花税/总市值/未指定股操作参数、复制委托清单；持仓表增加复制代码与跳个股详情快捷动作                        |
| 组合 | 扩展 `src/api/portfolio.ts` 可选类型字段，新增 `src/utils/format-portfolio.ts` 统一组合金额/比例/新鲜度格式化；通过 ESLint 与 `yarn build` 验证                                |

### 前端变更（用户管理重构）

| 模块     | 变更说明                                                                                                                                                  |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 用户管理 | 按用户决策固化本期范围：不做邀请流程；不允许操作另一名 SUPER_ADMIN；登录类安全事件并入审计；批量允许部分成功；软删除不级联业务资产                        |
| 用户管理 | 重构 `/user-manage`：新增 KPI 摘要、URL 状态、排序、注册时间区间、包含已删除、行选择与批量操作条、ConfirmDialog 统一危险动作                              |
| 用户管理 | 增强创建/编辑/重置密码：强密码生成与校验、一次性密码展示/清除、配额「不限 / 具体数字」控件、角色调整弹窗                                                  |
| 用户管理 | 增强审计日志：快捷时间区间、结果筛选、字段级 diff 展示、JSON fallback、请求 AbortController 竞态治理                                                      |
| 用户管理 | 补充 mock 数据与本地图标注册；完成 UI guideline 快速检查（无硬编码颜色/rgba/小字号命中，icon-only 按钮补 `aria-label`），通过 ESLint 与 `yarn build` 验证 |

### 前端变更（多策略对比重构 · 第一批）

| 模块       | 变更说明                                                                                                                                                              |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 多策略对比 | 设计确认落地：Q1 包含历史列表页；Q2 成本/约束默认统一 + 策略级覆盖；Q3 滚动/月度由后端新端点；Q4 相关性改为后端计算；Q5 等权包络线采用简单算术等权日收益 + 按日再平衡 |
| 多策略对比 | 新增 `/backtest/comparison` 历史列表页与 `ComparisonListCard`：KPI 摘要、状态筛选、关键词搜索、分页、查看详情、复制配置重跑、取消、删除                               |
| 多策略对比 | 扩展 `src/api/backtest.ts`：新增 `list/config/rolling/monthly/correlation/envelope/append/cancel/delete` 等多策略对比增强端点类型与函数                               |
| 多策略对比 | 创建页支持从历史列表复制配置预填，并将创建响应 `jobId` 写入 `sessionStorage`；详情页支持刷新后恢复 jobId、PARTIAL 状态和已完成数据展示                                |
| 多策略对比 | 路由与导航接入：新增 `/backtest/comparison` 薄页面，回测导航拆分为「对比历史 / 新建对比」；通过 ESLint 与 `yarn build` 验证                                           |

### 前端变更（回测任务历史重构）

| 模块         | 变更说明                                                                                                                                                 |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 回测任务历史 | 重构 `/backtest/runs`：新增 URL 状态 Hook、DatePicker 起止日期、状态/策略/归档/关键词筛选、排序状态、筛选计数与清空                                      |
| 回测任务历史 | 新增 KPI 摘要条、固定专业列组、耗时列、失败原因摘要、标签 chip、runId 复制按钮、RUNNING/QUEUED WebSocket 进度局部更新                                    |
| 回测任务历史 | 行内操作支持查看、取消、复制重跑；复制重跑改为先拉详情并完整预填工作台参数，避免旧版只带 `strategyType` 的参数丢失                                       |
| 回测任务历史 | 新增批量操作条：多选后可批量加入对比；归档/删除/打标签在后端未就绪前以禁用态提示；对比创建页支持从多个 runId 自动预填策略与通用参数                      |
| 回测任务历史 | 扩展 `src/api/backtest.ts` 类型与未来端点函数；新增 `use-backtest-run-list-state` 单元测试；补充本地 Iconify 图标并通过 ESLint、单测与 `yarn build` 验证 |

### 前端变更（Walk-Forward 验证重构）

| 模块         | 变更说明                                                                                                                                                   |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Walk-Forward | 重构 `/backtest/walk-forward` 列表：新增 URL 同步筛选/排序/分页、KPI 摘要、RUNNING/QUEUED 自动轮询、WFE 与稳健性列、复制 ID 与删除入口（后端未就绪时提示） |
| Walk-Forward | 重构创建页：WF/Rolling 共用模式 Tabs、参数组合预算、窗口预览、Purge/Embargo/minOosTrades 防泄漏字段，Rolling 模式接入 `createRollingBacktest`              |
| Walk-Forward | 重构详情页：总览/稳健性/窗口明细/配置复盘 4 Tab，新增 WFE、负窗口占比、IS-OOS 衰减、参数稳定性与窗口表现分布                                               |
| Walk-Forward | 新增窗口钻取 Drawer 与未来端点函数；后端缺失时使用列表窗口数据降级展示，并明确标注待支持的成交/持仓/调仓明细                                               |
| Walk-Forward | 扩展 `src/api/backtest.ts` WF 类型与未来端点；新增 `walk-forward-utils` 纯函数和 6 条单元测试；通过 ESLint、单测与 `yarn build` 验证                       |

### 前端变更（信号历史重构）

| 模块     | 变更说明                                                                                                                                   |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 信号历史 | 重构 `/strategy/signal/history`：新增 URL 状态筛选、工具栏草稿态、DatePicker + 快捷区间、摘要卡、交易日分组卡、HOLD 默认隐藏与单日抽屉     |
| 信号历史 | 信号行补齐 T+1/T+5/T+20 前瞻收益、当前/目标权重、置信度条、所选窗口超额收益、因子贡献展开；正收益红、负收益绿，数字列使用 tabular-nums     |
| 信号历史 | 新增 `/strategy/signal/history/compare` 多策略对比页，最多 3 个已激活策略，支持 T+1/T+5/T+20 窗口；旧 `/signal/history/compare` 路径重定向 |
| 信号历史 | 扩展 `src/api/signal.ts` 类型与 `history/detail/compare` POST API；补充 MSW mock 数据和 `signal-return-text` 单元测试                      |

---

## 变更日志（2026-04-30 → 2026-05-01）

### 前端变更（异动监控重构）

| 模块     | 变更说明                                                                                                                                                                                            |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 异动监控 | 全量重构 `/alert/anomalies`：新增 `sections/alert/anomalies/` 模块（HeaderBar / FiltersBar / SummaryRow / TableWorkbench / DetailDrawer / EmptyState / AddWatchlistDialog）                         |
| 异动监控 | 引入 `useAnomalyMonitorState` Hook 统一管理筛选/分页/排序/URL 同步/AbortController/WebSocket 自动刷新                                                                                               |
| 异动监控 | 表格升级：强度排序 + isNew/连板徽章 + 共振 +N Chip + 多选批量加自选/复制代码 + 分页器（20/50/100）                                                                                                  |
| 异动监控 | 详情 Popover 升级为右侧 Drawer：分组证据链（VOLUME_SURGE / CONSECUTIVE_LIMIT_UP / LARGE_NET_INFLOW 各自指标块）+ 同股共振 + 历史轨迹 + NaN 安全格式化                                               |
| 异动监控 | 管理员"立即扫描"内嵌至 HeaderBar                                                                                                                                                                    |
| 异动监控 | 类型/颜色/口径单一来源：`anomaly-type-config.ts`（含 strength fallback、severity meta、tradeDate 互转）                                                                                             |
| 异动监控 | 后端待扩展（前端已优雅降级）：`stats` 聚合（KPI 卡片）、`severity` / `isNew` / `coincidentTypes` / `industryName` / `isInWatchlist` 字段、`/api/alert/anomalies/detail` 端点、历史 `tradeDate` 重扫 |

---

## 变更日志（2026-04-16 → 2026-04-30）

### 前端变更（股票列表重构）

| 模块     | 变更说明                                                                                                          |
| -------- | ----------------------------------------------------------------------------------------------------------------- |
| 股票列表 | 行业/地域改为 Autocomplete 多选；新增"高流动性/百亿以上/高股息"快捷条件                                           |
| 股票列表 | 新增列配置 Popover（12 列可选 + 重置），列偏好写入 localStorage（`stock-list:columns:v1`）                        |
| 股票列表 | 跨页多选 + 批量加入自选股（`/api/watchlist/stocks/batch`）；行 hover 快捷动作（复制代码 / 加入自选股 / 查看详情） |
| 股票列表 | 新增空状态 `StockEmptyState`、骨架屏 `StockSkeletonRows`、底部多选操作条 `StockBulkActionBar`                     |
| 股票列表 | 后端待扩展：`/api/stock/list` 需支持 `industries[]`、`areas[]` 多值参数（前端已发送，单值时回退到旧字段）         |

---

## 变更日志（2026-04-12 → 2026-04-16）

### 前端变更（后端优化适配 — 全量实现）

| 模块         | 变更说明                                                                                                                                                                                                                                                                                                                                                             |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WebSocket    | 重连增强（10次/30s上限）、离线事件回放、连接状态指示器（Header）                                                                                                                                                                                                                                                                                                     |
| 定时报告     | 新增 api/report.ts 定时报告 CRUD 5 端点、报告列表页增加"定时报告"Tab                                                                                                                                                                                                                                                                                                 |
| 市场概览     | 新增 daily_info 每日全景快照卡片（成交额/涨停/跌停/封板率/连板/换手）                                                                                                                                                                                                                                                                                                |
| 预警监控     | 新增涨跌停明细页（/alert/limit-list）、连板龙虎榜、筛选统计；2026 重构升级 v2：3 Tab（今日/次日/历史）、6 KPI 摘要+迷你 Sparkline、主线行业 Bar、封板时点 5 分钟直方图、UP+DOWN 连板梯队、9 列表（封板形态/封板节奏/操作）、个股抽屉、URL 同步筛选；依赖后端 BE-1~14（含 limit-summary、limit-next-day-perf、ext.industry/concepts/streakStatus/sealPattern 等字段） |
| 筹码分布     | concentration90/70 字段增强、获利/套牢分色、平均成本标记线、数据源标识                                                                                                                                                                                                                                                                                               |
| 机构行为分析 | 新增 api/fund.ts（3 端点）、股票详情分析"机构持仓"Tab                                                                                                                                                                                                                                                                                                                |
| 分钟 K 线    | 新增 minute-kline API、K 线周期扩展至 1分/5分/15分/30分/60分 + 日期选择器                                                                                                                                                                                                                                                                                            |
| 行业轮动     | 新增 sector-daily API、动量图表增加成交额 tooltip                                                                                                                                                                                                                                                                                                                    |
| 事件日历     | EventType 扩展至 7 种（+IPO/CONVERTIBLE/SHAREHOLDER）、筛选/颜色映射更新；2026-05-01 重构升级（月历/时间线/表格三视图、影响力评分、历史趋势对比、批量订阅、EVENT\_\* 事件订阅规则）                                                                                                                                                                                  |

<details>
<summary>历史变更（2026-04-11 → 2026-04-12 early）</summary>

### 后端变更（+41 端点，+1 模块）

| 模块          | 上次端点数 | 当前端点数 | 变化 | 新增端点说明                                                                               |
| ------------- | ---------- | ---------- | ---- | ------------------------------------------------------------------------------------------ |
| **Signal**    | N/A        | **5**      | 🆕+5 | 新模块：策略信号激活/停用/列表/最新信号/信号历史                                           |
| Portfolio     | 20         | 26         | +6   | apply-backtest、rebalance-plan、performance、drift-detection、trade-log、trade-log/summary |
| Backtest      | 18         | 22         | +4   | monte-carlo、attribution、cost-sensitivity、param-sensitivity（含 result）                 |
| Tushare Admin | 13         | 17         | +4   | sync-logs/summary、retry-queue、retry-queue/reset、sync-status-overview                    |
| Market        | 17         | 20         | +3   | sector-flow、concept/list、concept/members                                                 |
| Stock         | 24         | 27         | +3   | search、main-money-flow、share-capital、concepts（-1 旧接口合并）                          |
| Factor        | 21         | 23         | +2   | analysis/decay、optimization                                                               |
| Strategy      | 8          | 10         | +2   | versions、compare-versions                                                                 |
| Report        | 6          | 7          | +1   | strategy-research                                                                          |
| Watchlist     | 13         | 14         | +1   | stocks/reorder                                                                             |

### 前端变更（+34 端点覆盖）

| 模块      | 上次状态    | 当前状态             | 说明                                                        |
| --------- | ----------- | -------------------- | ----------------------------------------------------------- |
| Alert     | ❌ 完全缺失 | ✅ 完整覆盖（8/8）   | 新增 api/alert.ts + alert section（日历/价格预警/异动）     |
| Portfolio | ❌ 完全缺失 | ⚠️ 部分覆盖（20/26） | 新增 api/portfolio.ts + portfolio section，6 个新端点未接入 |

</details>

<details>
<summary>历史变更（2026-04-09 → 2026-04-11）</summary>

| 模块                  | 上次状态    | 当前状态    | 说明                                        |
| --------------------- | ----------- | ----------- | ------------------------------------------- |
| Backtest Walk-Forward | ⚠️ 部分覆盖 | ✅ 完整覆盖 | 新增 WF create/list/detail/equity 4 端点    |
| Backtest Comparison   | ⚠️ 部分覆盖 | ✅ 完整覆盖 | 新增 comparison create/detail/equity 3 端点 |

</details>

<details>
<summary>历史变更（2026-04-03 → 2026-04-09）</summary>

| 模块           | 上次状态    | 当前状态    | 说明                                               |
| -------------- | ----------- | ----------- | -------------------------------------------------- |
| Strategy       | ❌ 完全缺失 | ✅ 完整覆盖 | 新增 section/list/detail/create/clone/delete/run   |
| Tushare Admin  | ⚠️ 部分覆盖 | ✅ 完整覆盖 | Tab 化增强：同步日志、数据质量、缓存统计、重试队列 |
| User 自助功能  | ⚠️ 部分覆盖 | ✅ 完整覆盖 | change-password、profile/update 已接入 Profile 页  |
| User 审计日志  | ❌ 缺失     | ✅ 完整覆盖 | audit-log-tab 已接入用户管理页                     |
| Strategy-Draft | ⚠️ 无独立页 | ✅ 已整合   | 回测工作台草稿箱随 Strategy 模块完整交付           |

</details>

---

## 一、总览

| 状态        | 模块数 | 说明                           |
| ----------- | ------ | ------------------------------ |
| ✅ 完整覆盖 | 21     | 全部模块均已达成 100% 端点覆盖 |
| ⚠️ 部分覆盖 | 0      | —                              |
| ❌ 完全缺失 | 0      | —                              |

### 各模块端点明细

| 模块                  | 后端    | 前端    | 覆盖率   | 状态 |
| --------------------- | ------- | ------- | -------- | ---- |
| Auth                  | 4       | 4       | 100%     | ✅   |
| Alert                 | 8       | 8       | 100%     | ✅   |
| Backtest              | 22      | 22      | 100%     | ✅   |
| Event Study           | 9       | 9       | 100%     | ✅   |
| Factor                | 23      | 23      | 100%     | ✅   |
| Heatmap               | 3       | 3       | 100%     | ✅   |
| Index                 | 3       | 3       | 100%     | ✅   |
| Industry Rotation     | 7       | 7       | 100%     | ✅   |
| Market                | 20      | 20      | 100%     | ✅   |
| Pattern               | 3       | 3       | 100%     | ✅   |
| Portfolio             | 26      | 26      | 100%     | ✅   |
| Report                | 7       | 7       | 100%     | ✅   |
| Research Note         | 7       | 7       | 100%     | ✅   |
| Screener Subscription | 8       | 8       | 100%     | ✅   |
| Signal                | 5       | 5       | 100%     | ✅   |
| Stock                 | 27      | 27      | 100%     | ✅   |
| Strategy              | 10      | 10      | 100%     | ✅   |
| Strategy Draft        | 6       | 6       | 100%     | ✅   |
| Tushare Admin         | 17      | 17      | 100%     | ✅   |
| User                  | 11      | 11      | 100%     | ✅   |
| Watchlist             | 14      | 14      | 100%     | ✅   |
| **合计**              | **243** | **243** | **100%** |      |

---

## 二、各模块实现详情

> 所有 243 个后端端点均已在前端完成 API 接入 + UI 实现。以下按模块汇总各端点覆盖情况。

### Auth（4/4）✅

captcha · login · refresh · logout

### Alert（8/8）✅

calendar/list · price-rules/list · price-rules(create) · price-rules/update · price-rules/delete · price-rules/scan · anomalies/list · anomalies/scan

### Backtest（22/22）✅

strategy-templates · runs(create) · runs/validate · runs/list · runs/detail · runs/equity · runs/trades · runs/positions · runs/rebalance-logs · runs/cancel · walk-forward/runs(create) · walk-forward/runs/list · walk-forward/runs/detail · walk-forward/runs/equity · comparisons(create) · comparisons/detail · comparisons/equity · rolling/runs · runs/monte-carlo · runs/attribution · runs/cost-sensitivity · runs/param-sensitivity(+result)

### Event Study（9/9）✅

event-types · events · analyze · signal-rules(create) · signal-rules/list · signal-rules/update · signal-rules/delete · signal-rules/scan · signals

### Factor（23/23）✅

library · detail · values · analysis/ic · analysis/quantile · analysis/decay · analysis/distribution · analysis/correlation · screening · custom/create · custom/test · custom/update · custom/delete · custom/precompute · backtest/submit · backtest/attribution · backtest/save-as-strategy · analysis/orthogonalize · analysis/fama-macbeth · optimization · admin/precompute · admin/backfill · admin/precompute/status

### Heatmap（3/3）✅

data · snapshot/trigger · snapshot/history

### Index（3/3）✅

list · daily · constituents

### Industry Rotation（7/7）✅

return-comparison · momentum-ranking · flow-analysis · valuation · overview · detail · heatmap

### Market（20/20）✅

index-quote · index-trend · sentiment · change-distribution · sentiment-trend · sector-ranking · volume-overview · valuation · valuation-trend · money-flow · money-flow-trend · sector-flow-ranking · sector-flow-trend · hsgt-flow · hsgt-trend · main-flow-ranking · stock-flow-detail · sector-flow · concept/list · concept/members

### Pattern（3/3）✅

templates(GET) · search · search-by-series

### Portfolio（26/26）✅

create · list · detail · update · delete · holding/add · holding/update · holding/remove · pnl/today · pnl/history · risk/industry · risk/position · risk/market-cap · risk/beta · rule/list · rule/upsert · rule/update · rule/delete · risk/check · risk/violations · apply-backtest · rebalance-plan · performance · drift-detection · trade-log · trade-log/summary

### Report（7/7）✅

backtest · stock · portfolio · strategy-research · list · detail · delete

> **重构状态（2026-05-01）**：完成"量化报告-重构-前端设计"全量落地。智能下拉选源（runId / tsCode / portfolioId 不再裸输入），生成进度条 + 6 类错误码翻译，笔记面板防抖自动保存，分享链接（TTL+吊销，后端缺失时优雅降级），浏览器打印，列表 Toolbar（关键词/状态多选/按系列折叠）+ IconButton 行动组，详情头部彩色色条 + Hero。
> 后端待补能力（前端类型与 UI 已预埋，缺失字段降级）：
>
> - 报告 `version` / `paramsHash` / `progress` / `errorCode` / `notes` / `notesUpdatedAt`
> - 端点：`saveReportNotes` / `regenerateReport` / `createReportShareLink` / `listReportShareLinks` / `revokeReportShareLink` / `diffReports`
> - 回测扩展指标：`volatility / downsideDeviation / informationRatio / beta / alpha / benchmarkReturn / benchmarkAnnualReturn / turnover / hhi`、`benchmarkCurve`、`yearlyReturns`、trade `commission/slippage/tax/fillType`
> - 个股 `valuation` 历史 + `peerComparison`；组合 `attribution` + `tracking`；策略 `factorExposure` / `parameterSensitivity` / `rollingStability`

### Research Note（7/7）✅

list · detail · create · update · delete · tags · by-stock

### Screener Subscription（8/8）✅

list · create · update · delete · pause · resume · run · logs

> **重构状态（2026-04-30）**：前端已完成 KPI 总览、URL 化筛选/排序、5 分钟冷却倒计时、ERROR 智能提示、全局通知联动。
> 受限于后端能力，暂不支持的能力（已在 UI 中以只读/隐藏方式处理）：
>
> - `update` 不接受 `filters` / `sortBy` / `sortOrder` / `strategyId`，编辑面板仅开放 name 与 frequency
> - `pause` / `resume` 仅返回 `{message}`，前端通过乐观更新 + list 刷新兜底
> - 无 `/detail` 端点，详情通过 list + filter 实现
> - 日志缺股票元数据（name/industry/close/pctChg）
> - 无 `screener_subscription_failed` WebSocket 事件
> - 调度未走交易日历

### Signal（5/5）✅

strategies/activate · strategies/deactivate · strategies/list · latest · history

### Stock（27/27）✅

list · screener · screener/presets · industries · areas · screener/strategies/list · screener/strategies(create) · screener/strategies/update · screener/strategies/delete · detail/overview · detail/chart · detail/money-flow · detail/today-flow · detail/financials · detail/shareholders · detail/dividend-financing · detail/financing · detail/financial-statements · detail/analysis/technical · detail/analysis/timing-signals · detail/analysis/chip-distribution · detail/analysis/margin · detail/analysis/relative-strength · search · detail/main-money-flow · detail/share-capital · detail/concepts

### Strategy（10/10）✅

create · list · detail · update · delete · clone · run · schemas · versions · compare-versions

### Strategy Draft（6/6）✅

list · detail · create · update · delete · submit

### Tushare Admin（17/17）✅

plans · sync · cache/stats · quality/check · quality/report · quality/gaps · validation-logs · quality/cross-check · quality/summary · quality/repair · quality/repair-status · quality/health · sync-logs · sync-logs/summary · retry-queue · retry-queue/reset · sync-status-overview

### User（11/11）✅

profile/detail · list · create · detail · update · update-status · reset-password · delete · profile/change-password · profile/update · audit-log/list

### Watchlist（14/14）✅

list · create · update · delete · reorder · stocks/list · stocks/add · stocks/batch-add · stocks/update · stocks/remove · stocks/batch-remove · stocks/reorder · summary · overview

---

## 三、参考设计文档

| 模块                                   | 设计文档                                                                                                         |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 回测工作台                             | [回测工作台-前端设计](design/回测工作台-前端设计.md)                                                             |
| 回测高级功能                           | [回测高级功能-前端设计](design/回测高级功能-前端设计.md)                                                         |
| 因子市场                               | [因子市场-前端设计](design/因子市场-前端设计.md)                                                                 |
| 市场概览                               | [市场概览-前端设计](design/市场概览-前端设计.md)                                                                 |
| 市场概念板块 / 股票详情增强 / 策略版本 | [市场概念板块与股票详情增强与策略版本管理-前端设计](design/市场概念板块与股票详情增强与策略版本管理-前端设计.md) |
