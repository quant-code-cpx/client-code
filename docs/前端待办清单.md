# 前端仓库 (client-code) — 优化与缺失功能 TODO List

> 基于对前端仓库的全面审视，按优先级和分类整理。
> 技术栈：React 19 + TypeScript 5.8 + MUI 7 + Vite 6 + ApexCharts + Socket.IO
>
> 格式：`- [ ]` 待完成，`- [x]` 已完成。

---

## 一、测试（当前测试覆盖率 0% → 目标 ≥ 50%）

> 详见 [测试框架与计划-前端设计](design/测试框架与计划-前端设计.md)

### Phase 1：单元测试 — 核心逻辑层（P0）

- [x] 搭建测试框架（Vitest + React Testing Library + jsdom + MSW）
  - [x] 安装依赖：vitest, @testing-library/react, jest-dom, user-event, jsdom, msw, coverage-v8
  - [x] 创建 vitest.config.ts 配置
  - [x] 创建 src/test/setup.ts 和 src/test/test-utils.tsx
  - [x] 添加 package.json 脚本：test / test:watch / test:coverage / test:ui
- [x] 工具函数单元测试（src/utils/）
  - [x] format-number.test.ts — fNumber / fCurrency / fPercent / fShortenNumber（30 tests）
  - [x] format-number.test.ts — fWanYuan / fQianYuan / fWanYi / fPctChg / fRatePercent
  - [x] format-time.test.ts — fDate / fDateTime / fToNow / 无效日期处理（10 tests）
- [x] Auth Reducer 单元测试（src/auth/auth-reducer.test.ts，8 tests）
- [x] API Client tokenStorage 测试（src/api/client.test.ts，4 tests）
- [ ] API Client 请求逻辑测试
  - [ ] apiClient.post: 正常请求、携带 Bearer 头
  - [ ] 401 自动刷新 → 重试
  - [ ] 刷新失败 → 触发 onUnauthorized 回调
  - [ ] 并发 401 请求排队刷新
  - [ ] parseResponse 解包 { code, data } 格式
  - [ ] 非 JSON 响应处理
- [ ] Auth Provider 集成测试
  - [ ] 初始化 refresh token → AUTH_SUCCESS
  - [ ] refresh 失败 → AUTH_FAILURE
  - [ ] signIn / signOut / loadProfile 方法
  - [ ] BroadcastChannel 跨标签页同步
- [ ] 权限系统测试（src/permission/）
  - [ ] ROLE_LEVEL 层级验证
  - [ ] hasRole / hasMinRole / canManage 权限矩阵
  - [ ] HasPermission 组件条件渲染
- [ ] API 模块测试（MSW Mock）
  - [ ] auth.test.ts — login / getCaptcha / refresh / logout
  - [ ] stock.test.ts — stockApi.query 分页参数
  - [ ] market.test.ts — 指数行情 / 情绪 / 资金流向
  - [ ] backtest.test.ts — 创建 / 查询 / 详情

### Phase 2：组件测试 — UI 组件层（P1）

- [ ] 通用 UI 组件测试
  - [ ] Label: variant / color 渲染
  - [ ] Iconify: 图标渲染、宽高属性
  - [ ] Logo: 链接跳转、禁用模式
  - [ ] Scrollbar: 内容渲染、样式透传
  - [ ] Chart: useChart 配置合并、Loading / Legends
  - [ ] StockSearchAutocomplete: 搜索防抖、选择交互
- [ ] 登录表单测试
  - [ ] 表单渲染（账号/密码/验证码输入框）
  - [ ] 提交表单 → 调用 authApi.login
  - [ ] 登录失败错误提示

### Phase 3：集成测试 — 路由与跨模块（P2）

- [ ] 路由守卫测试
  - [ ] 未登录 → 重定向到 /sign-in
  - [ ] 已登录 → 正常渲染子组件
  - [ ] isLoading 期间显示加载状态
- [ ] ErrorBoundary 测试
  - [ ] 子组件报错 → 显示 fallback UI
- [ ] Context 集成测试
  - [ ] SyncNotificationContext: WebSocket 事件 → 通知更新
  - [ ] 通知列表上限（max 50）、标记已读

### Phase 4：E2E 测试（P3）

- [ ] 搭建 Playwright 框架
- [ ] 登录 → 首页 流程
- [ ] 股票搜索 → 详情 流程
- [ ] 回测提交 → 查看结果 流程
- [ ] 选股筛选 → 结果查看 流程

### 覆盖率与 CI

- [ ] 配置覆盖率阈值（Phase 1 完成后 ≥ 15%，最终 ≥ 50%）
- [ ] 集成到 GitHub Actions CI（`.github/workflows/ci.yml`）


---

## 二、CI/CD（当前无 GitHub Actions 工作流）

- [ ] 添加 CI 工作流：lint → type-check → build（`.github/workflows/ci.yml`）
- [ ] 添加 PR 预览部署（Vercel Preview Deployments）
- [ ] 添加 Bundle Size 检查（防止打包体积膨胀）
- [ ] 添加 Lighthouse CI（性能、可访问性评分门禁）

---

## 三、数据获取与状态管理

- [ ] 引入数据获取库（推荐 TanStack Query / SWR）：
  - 请求缓存与去重
  - 自动重试与刷新
  - 加载 / 错误状态管理
  - 乐观更新
- [ ] 为复杂页面添加状态管理（Zustand / Jotai）：
  - 股票筛选条件持久化
  - 回测参数缓存
  - 用户偏好设置
- [ ] 移除 `src/_mock/` 中不再使用的模拟数据

---

## 四、错误处理与用户体验

- [x] 添加全局 ErrorBoundary 组件（捕获渲染错误，显示友好提示和重试按钮）
- [ ] API 请求失败的统一错误提示（Toast / Snackbar）
- [ ] 网络断开提示与自动重连
- [ ] 请求超时处理（API Client 添加 timeout 配置）
- [x] 添加 Loading Skeleton 骨架屏（替代空白加载）
- [ ] 空状态页面设计（无数据 / 无搜索结果 / 无权限）
- [x] 操作确认弹窗（删除策略、删除用户等危险操作）

---

## 五、表单验证

- [ ] 引入表单验证库（Zod + React Hook Form）
- [ ] 登录表单添加前端校验（账号长度、密码强度、验证码格式）
- [ ] 回测参数表单校验（日期范围、资金范围、策略参数合法性）
- [ ] 选股策略表单校验（条件完整性、数值范围）
- [ ] 用户管理表单校验（邮箱格式、昵称长度）

---

## 六、性能优化

- [ ] 添加 Bundle 分析工具（`rollup-plugin-visualizer`）
- [ ] 长列表虚拟滚动（股票列表、回测历史列表）
- [ ] 图表数据量大时的降采样渲染
- [ ] 路由级别预加载（hover 时预取下一页数据）
- [ ] 图片 / 静态资源懒加载
- [ ] `React.memo` 优化高频重渲染组件
- [ ] Web Worker 处理大量数据计算（技术指标、筛选过滤）

---

## 七、安全加固

- [ ] API 请求添加 CSRF Token（如果后端启用）
- [ ] 添加 CSP（Content Security Policy）meta 标签或 Vercel Headers
- [ ] 敏感字段前端脱敏显示（如有涉及）
- [ ] XSS 防护：审查所有 `dangerouslySetInnerHTML` 使用
- [ ] 输入内容 sanitize（防止注入攻击）
- [x] 检查 `localStorage` / `sessionStorage` 中是否存储了敏感信息

---

## 八、UI / UX 增强

- [ ] 暗色模式支持（MUI ThemeProvider 切换）
- [ ] 响应式布局优化（当前可能仅适配桌面端）
- [ ] 键盘快捷键支持（`/` 聚焦搜索、`Esc` 关闭弹窗）
- [ ] 数据导出功能（表格数据导出为 CSV / Excel）
- [ ] K 线图交互增强：
  - 十字光标联动
  - 区间选择放大
  - 指标叠加切换
- [ ] 表格列排序 / 筛选 / 列可见性切换
- [ ] 拖拽排列 Dashboard 卡片
- [ ] 多语言支持（i18n，当前中英混合）
- [ ] 无障碍（Accessibility）：键盘导航、ARIA 标签、色彩对比度

---

## 九、WebSocket 实时功能增强

- [ ] Socket 连接状态 UI 指示器（已连接 / 断开 / 重连中）
- [ ] 断线自动重连机制（指数退避）
- [ ] 同步进度条增强（百分比、预计剩余时间）
- [ ] 按需订阅机制（只接收当前页面相关的推送）
- [ ] WebSocket 消息队列（离线期间消息缓存）

---

## 十、环境配置与开发体验

- [ ] 添加 `.env.example` 文件（含所有必需环境变量及注释）
  ```
  VITE_API_BASE_URL=http://localhost:3000
  VITE_WS_URL=http://localhost:3000
  ```
- [ ] 添加 `.env.development` 默认开发配置
- [ ] 添加 Husky + lint-staged（提交前自动 lint / format）
- [x] 更新 `README.md`（替换模板内容为项目实际说明）
- [ ] 添加 `CONTRIBUTING.md`（本地开发环境搭建指南）
- [ ] 统一包管理器（package.json 中 `packageManager: yarn`，但同时存在 `package-lock.json`）

---

## 十一、API 层优化

- [ ] API 响应类型与后端 Swagger DTO 保持同步（可考虑自动生成）
- [x] 请求取消机制（AbortController，页面离开时取消进行中的请求）
- [ ] 请求重试机制（网络抖动自动重试 1-2 次）
- [ ] 请求/响应拦截器增强：
  - 统一日志记录
  - 性能统计（请求耗时）
  - 错误上报
- [x] API Base URL 按环境自动切换

---

## 十二、监控与错误追踪

- [ ] 集成 Sentry（或类似工具）进行前端错误追踪
- [ ] 添加用户行为分析（可选，如 Mixpanel / 自建埋点）
- [ ] 添加 Web Vitals 监控（LCP / FID / CLS）
- [ ] 控制台错误和未捕获 Promise 拒绝统一上报

---

## 十三、代码质量

- [ ] 清理模板残留代码（blog、products 等 Minimal UI 模板页面）
- [ ] 抽取可复用 Hooks：
  - `useApiQuery` — 封装数据请求（loading / error / data）
  - `usePagination` — 分页逻辑
  - `useDebounce` — 搜索防抖
  - `useLocalStorage` — 本地存储状态
- [ ] 组件文档化（可选 Storybook）
- [ ] 统一文件命名规范（当前 kebab-case 为主，确保一致）
- [ ] TypeScript strict mode 审查（确保无隐式 any）

---

## 十四、缺失的功能页面

> 基于后端 20 个 Controller（202 端点）的最新盘点，详见 [前端功能缺口盘点](前端功能缺口盘点.md)。

### 已完成

- [x] 用户个人中心页面（修改密码、修改资料、查看配额）
- [x] 数据管理页面（同步日志查看、手动触发同步、数据完整性检查）

### 🔴 P0 — 核心缺失（后端已就绪，前端 0% 覆盖）

- [ ] **组合管理页面**（Portfolio）— 20 端点
  - 组合 CRUD（创建/列表/详情/编辑/删除）
  - 持仓管理（添加/更新/删除持仓）
  - 盈亏分析（当日浮动盈亏、历史净值曲线）
  - 风险分析（行业分布、仓位集中度、市值分布、Beta 系数）
  - 风控规则（CRUD + 检测 + 违规记录）
- [ ] **预警与监控页面**（Alert）— 8 端点
  - 事件日历（财报披露/限售解禁/除权除息/业绩预告）
  - 价格预警规则 CRUD
  - 异动监控记录查询

### 🔶 P1 — 重要功能

- [ ] **事件驱动研究页面**（Event Study）— 9 端点
  - 事件类型列表与事件查询
  - 事件影响分析（超额收益 AAR/CAAR）
  - 信号规则 CRUD + 信号历史查询
- [ ] **行业轮动分析页面**（Industry Rotation）— 7 端点
  - 行业收益对比 / 动量排名 / 资金流转 / 估值分位
  - 行业轮动总览 / 单行业详情 / 轮动热力图
- [ ] **因子自定义引擎**（Factor Custom）— 5 端点
  - 自定义因子 CRUD + 试算表达式 + 触发预计算
- [ ] **因子回测与归因**（Factor Backtest）— 2 端点
  - 因子策略一键回测 + 因子归因分析
- [ ] **因子高级分析**（Factor Analysis）— 2 端点
  - Fama-MacBeth 截面回归检验 + 因子正交化

### 🔷 P2 — 增强功能

- [ ] **量化报告页面**（Report）— 6 端点
  - 生成回测报告 / 个股研报 / 组合分析报告
  - 报告列表 / 详情 / 删除
- [ ] **K 线形态匹配页面**（Pattern）— 3 端点
  - 经典形态模板列表
  - 相似 K 线搜索（股票日线区间 / 自定义序列）
- [ ] 热力图页面（Heatmap）— 3 端点（含快照管理）
- [ ] 指数行情页面（Index）— 3 端点（日线行情 + 成分股权重）
- [ ] 回测滚动窗口（Backtest Rolling）— 1 端点

### ⬛ P3 — 低优先级

- [ ] 因子 Admin 管理接口（预计算/回补/状态）— 3 端点，可并入 Tushare 管理面板
- [ ] 系统设置页面（管理员：同步配置、系统参数）
- [ ] 通知中心页面（站内消息、价格预警通知）
- [ ] 帮助 / 文档页面（内嵌使用指南）

---

_最后更新：2026-04-11_
