# AI 研究工作台 v4 桌面重构验收（Round 3）

> 日期：2026-08-05  
> 范围：桌面端 UI-only 重构；不含移动端与调度入口  
> 设计基线：`ai-research-v4-16x9.png` 及模型、记忆、报告、通知、状态 5 张 16:9 分稿

## 1. 验收结论

通过。页面已按 Research Dossier + Live Trace 方向重构；API、POST-SSE、reducer、权限、模型/工具/记忆/报告/通知写操作保持原契约。

## 2. 用例与结果

| 用例 | 结果 | 证据 |
| --- | --- | --- |
| 1920 主工作区 | 通过 | 264px 会话轨、正文卷宗、320px 固定证据栏、固定 Composer |
| 1440 工作区 | 通过 | 正文宽度不低于 760px；证据改为 360px 桌面 Drawer；无横向滚动 |
| 会话列表 | 通过 | MUI X listbox、搜索、新建、选择与深链保持可用 |
| 消息与富内容 | 通过 | 研究命题右对齐；助手以 Research Dossier 展示；Markdown、引用、口径保持可用 |
| 运行态 | 通过 | 实际 SSE 展示 `planSummary`、阶段、`4 / 8` 进度；未展示私有思考链 |
| 真实发送 | 通过 | 创建会话 `cmsexywtb000zqd6lqfydad6d`，发送后收到“联调通过”终态回答 |
| 模型 | 通过 | AUTO/MANUAL、模型目录、能力、费用、可用状态、保存约束保持可用 |
| 长期记忆 | 通过 | 520px Drawer；空态、统计、创建、纠正、删除与二次确认回归通过 |
| 研究报告 | 通过 | 报告库与双栏保存预览；两阶段 token/幂等保存测试通过；标题不泄露 Markdown 标记 |
| 通知与投递 | 通过 | 大尺寸双栏 Dialog；渠道 CRUD/测试/启停、投递历史与重试逻辑保持可用 |
| 证据 | 通过 | 1920 固定栏、1440 Drawer；引用 URL、locator、Provenance 复用原数据 |
| 主题兼容 | 通过 | 移除 Agent 根容器与所有 Portal 的局部暗色锁定；亮/暗主题下主界面及模型、记忆、通知、报告、证据弹层均继承全局主题 |
| 异常与空态 | 通过 | 新建空态、会话加载失败、停止/失败回答、目录失败与空列表均有原地反馈 |

## 3. 自动化与工程校验

- Agent 组件/状态/Hook 测试：17 个文件、57 条用例通过；Agent JSON facade 与 POST-SSE 契约另有 2 个文件、30 条用例通过，合计 19 个文件、87 条用例。
- ESLint：`node_modules/.bin/eslint "src/**/*.{ts,tsx}"` 退出码 0，零输出。
- TypeScript + Vite：`yarn build` 通过，出现 `✓ built in 8.59s`。
- 浏览器控制台：1920/1440 全流程 `error` / `warn` 为 0。
- Web Interface Guidelines：补齐 Dialog 可访问名称、overscroll containment、标题换行与暗色原生控件作用域。

## 4. 边界

- `VITE_AGENT_RICH_BLOCKS_ENABLED=false` 时继续遵守现有功能开关，不强制展示富块与工具摘要。
- 未新增附件、模板、工具选择、单次记忆开关、私有思考链或调度入口。
- 构建仍有项目既存的大 chunk 提示；不影响本轮构建通过，也不是 lint 问题。
