# AI 研究工作台修复回归 Round 6（2026-08-07）

## 结论

在独立新会话 `cmsii9fkd001en19t3lkm132d` 中完成 4 条真实浏览器消息，均收到模型终态回答且携带 1 个匹配的引用来源。会话未复用 Round 5 的上下文，也没有残留“后台运行中”。

## 本轮真实回归

| 顺序 | 场景 | 浏览器实际结果 | 结论 |
| --- | --- | --- | --- |
| 1 | 可转债 BASIC | `get_convertible_bond_market(BASIC, 110059.SH)` 返回浦发转债基础信息、摘牌/零余额风险提示和“可转债市场数据”引用。 | 通过 |
| 2 | 资讯单端日期 | 仅传 `publishedAfter` 的 `get_market_news` 正常终态，页面返回 0 条、最新数据时间和禁用主题源限制。 | 通过 |
| 3 | 因子 IC 缺省日期 | `get_factor_analysis(IC, [pe_ttm])` 未传起止日期仍成功，页面给出自动补全区间说明、IC 均值约 -0.037、ICIR 约 -0.21 及“因子分析结果”引用。 | 通过 |
| 4 | 宏观数值与引用 | `get_macro_snapshot(GDP)` 在页面展示 2026Q1 GDP `334192.9` 亿元、同比 5.0%、数据限制和“宏观经济数据”引用。 | 通过 |

## 运行态核验

- 前端会话中每一条均先显示实时执行进度，完成后输入区恢复“发送问题”。
- 资讯工具的单端时间输入没有再触发底层参数校验失败；0 条结果被正确解释为覆盖限制，而非伪造资讯。
- 因子工具实际取得的默认窗口为 `2025-08-07` 至 `2026-08-07`，证明编排默认值已经生效。
- 宏观亿元数值与百分比展示均通过引用覆盖，未发生数值断言丢失。
- 后端 `app` 健康、`agent-worker` 运行中。

## 观察项

1. 因子最终自然语言只说明“系统自动补全区间”，未把具体起止日写入正文；工具审计数据存在完整日期。这不影响调用成功，但若产品验收要求正文逐字展示区间，应补强结论生成约束。
2. 因子结论生成在页面技术明细中出现第 2 次调用，最终记录耗时约 72.6 秒且标为“已修复一次”。尽管最终成功，这高于已配置的 60 秒预算；建议后续分辨它是结构化修复流程还是独立的供应商重试链路，并为长输出设独立限时指标。

