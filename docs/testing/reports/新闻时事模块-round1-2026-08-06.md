# 新闻时事模块 Round 1 执行报告

> 日期：2026-08-06，Asia/Shanghai  
> 契约：News API v1.2  
> 方法：业务场景优先、Outside-in TDD、RED → GREEN → Refactor  
> 结论：✅ 首个可用纵向切片通过；🟡 增强与真实 Provider canary 待续

## 1. 本轮交付

- 生成并校验 News OpenAPI 子契约与 TypeScript 类型。
- 新增三个 POST API adapter，透传 `AbortSignal`，保留 `partial/warnings/dataThrough`。
- 新增显式 `Asia/Shanghai` 时间精度格式化与 URL 白名单/日期半开区间转换。
- 新增列表与 coverage 独立 hooks，处理请求取消、陈旧响应、游标翻页、typed cursor 恢复、重复 cursor 防环和 article revision 合并。
- 新增筛选栏、新闻卡片、partial 告警、coverage 侧栏、详情 Drawer、空/错/加载/结束状态。
- 新增 `/market/news` 薄页面、受保护懒路由与“行情 → 新闻时事”导航。
- 对 `code=7014` 的安全默认关闭状态显示稳定公开文案，不渲染服务端 message。

## 2. TDD 执行结果

| 批次 | RED 场景 | GREEN 结果 |
| ---- | -------- | ---------- |
| API | 路径、Body 紧缩、signal、响应包络与数字错误码 | 通过 |
| 时间/URL | 四种时间精度、上海时区、90 日边界、白名单与深链 | 通过 |
| 列表 hook | 首屏/空/错、竞态、翻页、重试、typed cursor、去重、防环 | 通过 |
| coverage hook | 独立成功/失败、刷新中止、卸载清理 | 通过 |
| 组件 | 卡片键盘/外链、覆盖告警、三态 feed、详情与截断 | 通过 |
| 页面 | 默认请求、独立失败、空态、详情 URL、加载更多、筛选恢复、模块关闭 | 通过 |
| 路由 | 受保护 route 与导航顺序 | 通过 |

自动化汇总：**9 suites / 75 tests 全部通过**。

## 3. 工程门禁

| 门禁 | 结果 |
| ---- | ---- |
| News 定向 ESLint | 通过，零告警 |
| `npm run api:news:check` | 通过，无契约漂移 |
| `vite build` | 通过，News 页面生成独立 lazy chunk |
| 全仓 `tsc --noEmit` | 基线阻断：3 个既有主题/颜色文件共 5 个错误；News 增量无错误 |

全仓类型错误位于 `src/components/color-utils/color-preview.tsx`、`src/theme/core/palette.ts`、`src/theme/theme-presets.ts`。本轮未越界修改这些与 News 无关的用户改动。

## 4. 浏览器验收

- 使用已有登录会话从“行情”导航进入 `/market/news`，路由保持 SPA 鉴权上下文。
- 桌面宽屏：筛选 Card、8/4 主区与 coverage sticky 侧栏布局正常。
- 390×844：页面无横向溢出，范围、搜索、枚举、日期、开关与错误告警均可见。
- Compose 保持 `NEWS_ENABLED=false` 安全默认；真实请求返回 503/code7014 时，列表显示“新闻模块尚未启用”，coverage 独立显示“状态暂不可用”，页面无白屏。
- 验收结束后恢复原应用路由、浏览器 viewport 与标签状态。

## 5. 下一批 RED

- 自动探新与“有新内容”静态提示。
- 刷新失败保留旧列表、详情显式重试与 20 条 LRU 缓存。
- 长列表虚拟化与视觉日期分隔，不改变后端排序。
- 移动端紧凑筛选入口/Drawer，以及指定证券多选搜索适配。
- 启用 News 后的真实成功态、Provider 字段漂移、5 天 canary 与 E2E。
- 继续把 92 条业务 Oracle 中未执行的增强场景逐批转为 RED；未执行项不计为通过。
