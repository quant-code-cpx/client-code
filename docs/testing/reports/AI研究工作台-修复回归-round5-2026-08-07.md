# AI 研究工作台修复回归 Round 5（2026-08-07）

## 结论

Round 4 发现的可闭环代码与运行配置问题已修复并回归。真实 Chrome 登录态中，同一新会话完成可转债、资讯和因子 IC 三类工具调用；会话侧栏显示 `8 条消息` 且没有残留“后台运行中”。其中 8 条包含修复前一条故意复现的资讯失败消息，修复后的三次运行均为 `COMPLETED`。

## 本轮真实回归

| 场景 | 审计入参 / 页面结果 | 结果 |
| --- | --- | --- |
| 可转债 | `get_convertible_bond_market`：`BASIC` + `110059.SH` | 成功返回浦发转债、退市数据截止日和限制。 |
| 本地资讯 | 模型仅给出 `publishedAfter`；编排自动补入 `publishedBefore`，后端审计可见两个字段 | 成功返回 0 条、coverage 水位与已禁用主题源限制。 |
| 因子 IC | 模型仅给出 `analysis=IC`、`factorNames=[pe_ttm]`；编排补入 `2025-08-07` 至 `2026-08-07` | 成功返回 3,956 股票样本的 IC 序列、区间和限制。 |
| 会话侧栏 | 当前会话显示 `8 条消息`，浏览器 DOM 中 `后台运行中=false` | 成功。 |

## 修复内容

1. 会话投影使用本地已加载消息数与列表计数的较大值；发现终态 Run 时清除残留 active 标记。
2. 运行时工具目录启用 `get_convertible_bond_market` 与 `get_market_news`，并重建 app/agent-worker 注入配置。
3. Agent 编排保留因子 Facade 的严格语义：仅移除跨 analysis 的已知参数，并为 IC/分组/衰减分析补默认一年窗口；不会伪造相关性分析所需的第二个因子。
4. 对 `get_market_news` 的单端时间范围补齐另一端，保持底层“成对时间窗口”约束。
5. 事实摘要超长时保留首尾证据；引用校验支持原始亿元数值和小数转百分比展示。
6. 通过模型供应商控制台将 `deepseek-v4-flash` 调整为 `timeoutMs=60000`、`maxRetries=0`，并发布活动配置版本 `modelcfg_1786076447929_54f6529e`。

## 自动化与构建

- 后端：4 个定向 Jest 套件，37 项通过；`pnpm run build` 通过。
- 前端：2 个定向 Vitest 文件，21 项通过；全量 ESLint 自动修复后复检无输出；`yarn build` 通过。

## 保留边界

- `search_web` / `fetch_web_page` 保持禁用：当前 `AGENT_SEARCH_PROVIDER=disabled` 且 URL token secret 未配置，不能通过强开工具绕过外部数据安全门禁。
- 私有组合和回测数据仍缺少非空隔离夹具；不应把 `DATA_NOT_FOUND` 伪装为成功。
- 单因子 `CORRELATION` 不会由编排猜造第二因子，仍应由规划器生成合法业务输入。
