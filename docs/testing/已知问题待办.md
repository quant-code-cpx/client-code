# 已知问题待办（Testing Backlog）

> 本文件记录测试过程中发现、**当前尚未完全修复**的 33 个问题。
>
> 已闭环的 70 个问题（含历史 `FIX-001`～`FIX-034`）见[已解决问题归档](../archive/testing/已解决问题-截至2026-08-09.md)；执行证据见对应测试报告归档。
> 每个问题必须填写：模块、严重等级、根因分类、状态、下一步行动。

---

## 严重等级说明

| 级别 | 含义                                |
| ---- | ----------------------------------- |
| P0   | 阻断核心流程，需立即修复            |
| P1   | 主要功能受损，计划内修复            |
| P2   | 次要功能 / 展示缺陷，不影响核心使用 |
| P3   | 体验优化 / 边缘场景，后续迭代处理   |

## 根因分类说明

| 类型            | 含义                          |
| --------------- | ----------------------------- |
| `code-bug`      | 前端代码逻辑错误              |
| `data-missing`  | 后端数据库历史数据不足        |
| `data-coverage` | 数据口径 / 数据表覆盖范围不够 |
| `design-gap`    | 业务设计未明确该场景          |
| `backend-bug`   | 后端接口逻辑错误              |

---

## 开放问题列表

### QA-STK-02 · 选股器修改条件后未标记结果过期

| 字段 | 内容 |
| --- | --- |
| **模块** | 股票 → 选股器 → 估值条件 |
| **严重等级** | P1 |
| **根因分类** | `code-bug` |
| **发现版本** | 2026-08-09（真实浏览器回归） |
| **复现步骤** | 登录后进入 `/stock` → 打开「选股器」→ 选择「估值」→ 在 `PE TTM上限` 输入 `10`，不要点击「开始选股」。 |
| **实际表现** | 输入控件值已为 `10`，且没有向 `/api/stock/screener` 发出请求，但条件计数仍为 `0 条`，结果区仍显示「结果与执行条件一致」。 |
| **期望表现** | 修改条件后立即显示 dirty/待执行状态、正确条件数，并明确当前结果尚未应用新条件；仅在点击「开始选股」后更新结果。 |
| **根因分析** | 条件输入值已更新，执行请求也会正确携带 `maxPeTtm: 10`；但 dirty/活动条件计数和结果一致性提示没有跟随草稿条件状态更新。 |
| **状态** | 🔴 Open — 会误导用户把旧结果视为新条件结果。 |
| **下一步行动** | 前端修正草稿条件到 dirty 状态、活动条件计数和结果一致性提示的派生逻辑；补“修改阈值→不请求→显示待执行→点击执行”回归。 |

---

### BACK-TS-02 · 沪深 300 全历史基准尚未就绪

| 字段 | 内容 |
| --- | --- |
| **模块** | 股票详情 → 分析 → 技术指标 → 历史信号统计 |
| **严重等级** | P1 |
| **根因分类** | `data-coverage` |
| **发现版本** | 2026-08-03（技术信号统计真实联调） |
| **复现步骤** | 任意股票进入历史信号统计，保留默认「对比沪深 300 超额收益」并发起查询。 |
| **实际表现** | 后端返回 `TECHNICAL_SIGNAL_BENCHMARK_NOT_READY`；前端现已显示「沪深 300 基准数据未就绪」，并提供「关闭对标后重试」。 |
| **期望表现** | `000300.SH` 全历史日线与数据完整性门禁就绪后，返回每个样本的基准收益和超额收益。 |
| **根因分析** | 后端当前明确拒绝基准查询，避免用仅约两年的指数数据伪造全历史超额收益。 |
| **状态** | 🟡 Open — 原始收益和方向收益可用，超额收益暂不可用。 |
| **下一步行动** | 后端补齐 `000300.SH` 全历史数据及完整性校验；不得在前端自动把超额收益降级为 0。 |

---

### DATA-001 · 行业轮动 3M/6M/1Y 周期数据为空

| 字段           | 内容                                                                                                                                                                                                                                                                                                        |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 行业分析 → 行业轮动 tab                                                                                                                                                                                                                                                                                     |
| **严重等级**   | P1                                                                                                                                                                                                                                                                                                          |
| **根因分类**   | `data-missing`                                                                                                                                                                                                                                                                                              |
| **发现版本**   | 2026-05-17（Round 2 测试）                                                                                                                                                                                                                                                                                  |
| **复现步骤**   | 进入行业分析页 → 行业轮动 tab → 点击 3M / 6M / 1Y                                                                                                                                                                                                                                                           |
| **实际表现**   | 行业涨跌比显示 `0/0（共0个行业）`，全行业均值 `0.00%`，热力图空白                                                                                                                                                                                                                                           |
| **期望表现**   | 显示对应周期的行业涨跌排名、热力图和四象限 Top 10                                                                                                                                                                                                                                                           |
| **根因分析**   | `sector_capital_flows` 表当前只有 60 个交易日数据（2026-02-09 ~ 2026-05-15）。3M 计算需要 `rn=61`（61 个交易日前的收盘价作为起始基准），但最大 `rn=60`，导致 `close_60 = NULL`，过滤后返回空数组。6M/1Y 同理，前端对 `days` 做了 `Math.min(x, 60)` 上限保护，因此 6M 与 3M 发送相同的 `days=60`，同样失败。 |
| **状态**       | 🔴 Open — 阻塞于后端数据回填                                                                                                                                                                                                                                                                                |
| **下一步行动** | 后端补充 `sector_capital_flows` 历史数据至 ≥ 250 个交易日（覆盖 1Y），数据就绪后前端逻辑不需修改，该问题自动消除                                                                                                                                                                                            |

---

### DATA-002 · 行业详情抽屉 PE/PB 分位对东财子行业显示"--"

| 字段           | 内容                                                                                                                                                                             |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 行业分析 → 行业轮动 → 详情抽屉（概览卡片区）                                                                                                                                     |
| **严重等级**   | P2                                                                                                                                                                               |
| **根因分类**   | `data-coverage`                                                                                                                                                                  |
| **发现版本**   | 2026-05-17（Round 2 测试）                                                                                                                                                       |
| **复现步骤**   | 行业轮动 → 点击任意东财子行业（如"半导体设备"）→ 查看 PE 分位 / PB 分位                                                                                                          |
| **实际表现**   | PE 分位、PB 分位显示 `--`                                                                                                                                                        |
| **期望表现**   | 显示该行业当前 PE/PB 在过去一段时间内的百分位数                                                                                                                                  |
| **根因分析**   | `valuation_daily_medians` 表目前只覆盖申万二级行业（110 个），不包含东财细分主题板块（如"半导体设备"、"储能"等）。当按行业名查找该表时无命中，字段返回 `null`，前端渲染为 `--`。 |
| **状态**       | 🟡 Open — 需要产品侧决策是否引入东财口径估值数据                                                                                                                                 |
| **下一步行动** | 1. 产品确认：东财子行业估值分位是否有数据来源，是否需要展示；2. 如需展示，后端引入东财板块维度的 PE/PB 历史数据并建立新的估值分位计算逻辑                                        |

---

### DATA-003 · 1W 显示 497 个行业，1M 显示 496 个行业

| 字段           | 内容                                                                                                                                             |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| **模块**       | 行业分析 → 行业轮动 → 行业涨跌比卡片                                                                                                             |
| **严重等级**   | P3                                                                                                                                               |
| **根因分类**   | `data-missing`                                                                                                                                   |
| **发现版本**   | 2026-05-17（Round 2 测试）                                                                                                                       |
| **复现步骤**   | 行业轮动 → 切换 1W / 1M 对比行业总数                                                                                                             |
| **实际表现**   | 1W 期间显示"共 497 个行业"，1M 期间显示"共 496 个行业"                                                                                           |
| **期望表现**   | 相同交易日区间内两个周期的行业总数应一致，或差异需有业务解释                                                                                     |
| **根因分析**   | 推测某个行业在较短周期（5 个交易日）有记录，但在较长周期（20 个交易日）起始日之前尚未成立 / 上市，导致 20 日计算时被过滤。尚未确认具体行业名称。 |
| **状态**       | 🟢 Low Priority — 对业务影响极小，待查明后确认是否为预期行为                                                                                     |
| **下一步行动** | 数据库查询：对比 `sector_capital_flows` 中出现在 5 天窗口但不出现在 20 天窗口的行业名称，确认是否为新成立行业板块，如是则为预期行为可关闭        |

---

### BACK-002 · 定时报告列表接口未实现

| 字段           | 内容                                                                                 |
| -------------- | ------------------------------------------------------------------------------------ |
| **模块**       | 量化报告 `/research/report` → 定时报告 Tab                                           |
| **严重等级**   | P0                                                                                   |
| **根因分类**   | `backend-bug`                                                                        |
| **发现版本**   | 2026-05-17（研究工作台 Round 1）                                                     |
| **复现步骤**   | 进入 `/research/report` → 点击「定时报告」tab                                        |
| **实际表现**   | 接口 `POST /api/report/schedules/list` 返回 `Cannot POST /api/report/schedules/list` |
| **期望表现**   | 返回当前用户的定时报告任务列表                                                       |
| **状态**       | 🔴 Open — 后端接口缺失                                                               |
| **下一步行动** | 后端补充 `POST /api/report/schedules/list` endpoint，按设计文档返回定时任务分页数据  |

---

### OBS-ES-A · 事件查询返回缺股票名称

| 字段           | 内容                                                                                 |
| -------------- | ------------------------------------------------------------------------------------ |
| **模块**       | 事件研究 → 事件查询 / 信号规则 wizard 命中样本                                       |
| **严重等级**   | P2                                                                                   |
| **根因分类**   | `backend-bug`                                                                        |
| **发现版本**   | 2026-05-17                                                                           |
| **现象**       | 8 类事件查询结果与信号 wizard step3 预览结果均只有 `tsCode`，没有 `name / stockName` |
| **期望**       | 表格能展示「股票名称」列                                                             |
| **状态**       | 🟡 Open                                                                              |
| **下一步行动** | 后端在事件查询 service 中 join `stock_basic` 表，返回 `name` 字段                    |

---

### OBS-ES-B · 事件分析 tab 缺设计的「热力图+wizard」

| 字段           | 内容                                                                                 |
| -------------- | ------------------------------------------------------------------------------------ |
| **模块**       | 事件研究 → 事件分析 Tab                                                              |
| **严重等级**   | P2                                                                                   |
| **根因分类**   | `design-gap`                                                                         |
| **发现版本**   | 2026-05-17                                                                           |
| **现象**       | 当前仅有简单表单 + 结果页（AAR/CAAR + Top10），无「热力图点击事件类型展开 + wizard」 |
| **期望**       | 与 `docs/design/事件驱动研究-重构-前端设计.md` R2 一致                               |
| **状态**       | 🟡 Open — 与产品确认 R2 优先级                                                       |
| **下一步行动** | 排期 R2 交互重构（热力图 + 多步分析向导）                                            |

---

### BACK-003 · 事件日历限售解禁 floatRatio 重复 ×100 致比例溢出

| 字段           | 内容                                                                                                                                 |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| **模块**       | 预警监控 → 事件日历（AC）— 限售解禁事件 title                                                                                        |
| **严重等级**   | P1                                                                                                                                   |
| **根因分类**   | `backend-bug`                                                                                                                        |
| **发现版本**   | 2026-05-17                                                                                                                           |
| **现象**       | 月历/时间线/表格视图，限售解禁条目 title 显示 `限售解禁 821.49%` `限售解禁 3542.05%` `限售解禁 1687.50%` 等远超 100% 的离谱数值      |
| **根因**       | 后端 `alert-calendar.service.ts` 在拼接 title 时执行 `(floatRatio * 100).toFixed(2)`，但 `floatRatio` 已是百分数（如 8.21 表 8.21%） |
| **期望**       | 后端去除 `*100`，直接 `floatRatio.toFixed(2) + '%'`；或确认 tushare `share_float.float_ratio` 口径并统一前后端                       |
| **状态**       | 🟡 Open（前端无法本地修复，等后端）                                                                                                  |
| **下一步行动** | 提报后端修订 `alert-calendar.service.ts` title 构造逻辑                                                                              |

---

### OBS-AC-A · 事件日历同股同日大量重复条目

| 字段           | 内容                                                                                                                      |
| -------------- | ------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 预警监控 → 事件日历（表格/时间线）                                                                                        |
| **严重等级**   | P2                                                                                                                        |
| **根因分类**   | `data-coverage` / `design-gap`                                                                                            |
| **发现版本**   | 2026-05-17                                                                                                                |
| **现象**       | 表格视图 `航天南湖 2026-05-18` 一天有 15+ 行完全一致的「限售解禁 5.78%」条目；`华纬科技` `慧智微-U` 等同日同股累计 10+ 条 |
| **根因**       | 后端按 share_float 的 ann_date / 多批次明细返回，未在事件层聚合；前端列表未做去重/分组                                    |
| **期望**       | 后端按 (stock, eventType, eventDate) 聚合；或前端 list 按相同 key 折叠显示「同日 N 条 / 累计 X%」                         |
| **状态**       | 🟡 Open                                                                                                                   |
| **下一步行动** | 与后端商定聚合口径，前端 timeline/table 增加 group + 展开/收起                                                            |

---

### OBS-AA-B · 异动详情 history 条目 anomalyType 后端未识别值

| 字段           | 内容                                                                                                                |
| -------------- | ------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 预警监控 → 异动监控（AA）— 详情抽屉 · 近期同股异动轨迹                                                              |
| **严重等级**   | P2                                                                                                                  |
| **根因分类**   | `backend-bug` / `data-coverage`                                                                                     |
| **发现版本**   | 2026-05-17                                                                                                          |
| **现象**       | 抽屉「近期同股异动轨迹」条目 Label 显示「未知」、检测值显示原始 float `11.53950145558257`（未走单位格式化）         |
| **根因**       | 后端 detail/history 返回的 `anomalyType` 非 FE 三类 (`VOLUME_SURGE / CONSECUTIVE_LIMIT_UP / LARGE_NET_INFLOW`) 之一 |
| **期望**       | 后端统一三类枚举或对外字段映射；FE 已通过 FIX-020 兜底避免崩溃                                                      |
| **状态**       | 🟡 Open                                                                                                             |
| **下一步行动** | 与后端核对 history 返回口径；FE 后续如新增类型补到 `anomaly-type-config.ts`                                         |

---

### OBS-AL-C · 涨跌停明细「占流通」比例数值远超 100%

| 字段           | 内容                                                                                                             |
| -------------- | ---------------------------------------------------------------------------------------------------------------- |
| **模块**       | 预警监控 → 涨跌停明细（AL）— 主表「封单 / 占流通」列                                                             |
| **严重等级**   | P1                                                                                                               |
| **根因分类**   | `backend-bug`                                                                                                    |
| **发现版本**   | 2026-05-17                                                                                                       |
| **现象**       | Round 2（2026-05-23）确认：恒林股份 603661.SH sealRatio=1.173 → 显示「占流通 117.3%」；理论上限 100%             |
| **根因**       | 后端 `sealRatio` 字段数值 > 1.0（超出 0-1 范围），可能是 `fdAmount / floatMv` 单位错配（元 vs 万元）             |
| **期望**       | 后端统一 `sealRatio = 封单额(万) / 流通市值(万)` 确保在 0-1 范围内；FE 代码 `fPercent(sealRatio * 100)` 无需改动 |
| **状态**       | 🟡 Open — 后端数据问题未解决                                                                                     |
| **下一步行动** | 跟后端核对 `floatMv` 单位与计算公式，确保 `sealRatio ≤ 1.0`                                                      |

---

### OBS-FM-B · 因子相关性热力图大面积缺失（真实后端）

| 字段           | 内容                                                                                                                                                    |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 因子市场 → 因子相关性                                                                                                                                   |
| **严重等级**   | P2                                                                                                                                                      |
| **根因分类**   | `backend-data`                                                                                                                                          |
| **发现版本**   | 2026-05-17                                                                                                                                              |
| **现象**       | 真实后端下，热力图除对角线外几乎全部显示 `—`（ρ=null）；后端 `correlation.matrix[i]` 除对角线外全部为 null，表明因子值历史数据不足（pairwise 交集 < 3） |
| **补充说明**   | Demo 模式（MSW）下页面功能完全正常，FC-001~FC-015 全部 PASS（见测试报告 `docs/archive/testing/reports/因子相关性-round1-2026-05-23.md`）                        |
| **期望**       | 后端补齐历史因子值（需 ≥30 个交易日），前端展示前检查 `matrix[0][1]` 是否全 null 时可提示「历史数据准备中」                                             |
| **状态**       | 🟡 Open — 阻塞于后端数据回填                                                                                                                            |
| **下一步行动** | 后端优先补充因子值历史数据；前端可增加「样本不足，建议补充历史数据」提示卡                                                                              |

---

### OBS-FM-C · BE `/api/factor/admin/jobs` 契约与文档不一致

| 字段           | 内容                                                                                                                                                                                                      |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 因子管理 → 任务历史                                                                                                                                                                                       |
| **严重等级**   | P1                                                                                                                                                                                                        |
| **根因分类**   | `backend-bug` (contract drift)                                                                                                                                                                            |
| **发现版本**   | 2026-05-17                                                                                                                                                                                                |
| **现象**       | 当前 BE-1 返回的是覆盖度统计对象 `{tradeDate, factorCount, totalStocks, missingStocks, coverageRate, status:'OLD'}`，而设计文档与 FE 期望是 `AdminJobItem`（含 `jobId/type/operator/progress/createdAt`） |
| **期望**       | BE-1 按 `docs/archive/因子管理-重构-前端设计.md` 重新对齐字段                                                                                                                                              |
| **状态**       | 🟡 Open — FE 已做防御性适配（FIX-025）                                                                                                                                                                    |
| **下一步行动** | 后端补齐 jobs 表 + 端点；FE 修复后可移除 `adminJobList` 中的过滤逻辑                                                                                                                                      |

---

### OBS-FM-D · 因子选股结果 0 命中（数据稀疏）

| 字段           | 内容                                                                                 |
| -------------- | ------------------------------------------------------------------------------------ |
| **模块**       | 因子市场 → 因子选股                                                                  |
| **严重等级**   | P2                                                                                   |
| **根因分类**   | `backend-data`                                                                       |
| **发现版本**   | 2026-05-17                                                                           |
| **现象**       | 默认条件（pe_ttm > 5）在最新交易日返回 0 命中；与 OBS-FM-B 同源（BE 因子值历史稀疏） |
| **期望**       | 历史因子值补齐后命中率正常；前端可在 0 命中时给出「请调整阈值/确认数据日期」提示     |
| **状态**       | 🟡 Open                                                                              |
| **下一步行动** | 后端补数据；前端可优化空态文案                                                       |

---

### OBS-FM-E · 因子管理「调度配置」「审计日志」端点 500

| 字段           | 内容                                                                                    |
| -------------- | --------------------------------------------------------------------------------------- |
| **模块**       | 因子管理 → 调度配置 / 审计日志                                                          |
| **严重等级**   | P2                                                                                      |
| **根因分类**   | `backend-bug`                                                                           |
| **发现版本**   | 2026-05-17                                                                              |
| **现象**       | 两 tab 加载时 alert "加载调度配置失败 / 加载审计日志失败"；页面不崩溃，错误状态正确兜底 |
| **期望**       | BE 完成端点实现                                                                         |
| **状态**       | 🟡 Open — FE 错误兜底已就绪                                                             |
| **下一步行动** | 后端补 `/api/factor/admin/schedules` 与 `/api/factor/admin/audit-logs`                  |

---

### OBS-SM-A · BE `/api/strategies/summary` 返回 404

| 字段           | 内容                                                                                            |
| -------------- | ----------------------------------------------------------------------------------------------- |
| **模块**       | 策略管理 → 我的策略 / 策略详情顶部 summary 条                                                   |
| **严重等级**   | P1                                                                                              |
| **根因分类**   | `backend-bug`                                                                                   |
| **发现版本**   | 2026-05-17                                                                                      |
| **现象**       | `apiClient.post('/api/strategies/summary')` 返回 404；FE 已 `.catch(() => {})` 静默吞错，不渲染 |
| **期望**       | BE 按 `docs/design/策略-重构-前端设计.md` 实现 summary 端点                                     |
| **状态**       | 🟡 Open — FE 容错已就绪                                                                         |
| **下一步行动** | 后端补端点                                                                                      |

---

### OBS-SM-B · BE `/api/strategies/performance` 返回 404

| 字段           | 内容                                                                                                              |
| -------------- | ----------------------------------------------------------------------------------------------------------------- |
| **模块**       | 策略管理 → 策略详情 → 业绩快照                                                                                    |
| **严重等级**   | P1                                                                                                                |
| **根因分类**   | `backend-bug`                                                                                                     |
| **发现版本**   | 2026-05-17                                                                                                        |
| **现象**       | BE 返回 `Cannot POST /api/strategies/performance`，FE 原始展示报文体验差；已通过 FIX-026 改为「业绩数据暂不可用」 |
| **期望**       | BE 实现业绩快照端点                                                                                               |
| **状态**       | 🟡 Open — FE 友好兜底已就绪（FIX-026）                                                                            |
| **下一步行动** | 后端补端点                                                                                                        |

---

### OBS-SM-C · BE `/api/signal/history/compare` 返回 404

| 字段           | 内容                                                                                                                    |
| -------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 策略管理 → 历史对比                                                                                                     |
| **严重等级**   | P1                                                                                                                      |
| **根因分类**   | `backend-bug`                                                                                                           |
| **发现版本**   | 2026-05-17                                                                                                              |
| **现象**       | BE 返回 `Cannot POST /api/signal/history/compare`，FE 原始展示报文体验差；已通过 FIX-027 改为「多策略对比接口暂不可用」 |
| **期望**       | BE 按 `docs/design/多策略对比-重构-前端设计.md` 实现端点                                                                |
| **状态**       | 🟡 Open — FE 友好兜底已就绪（FIX-027）                                                                                  |
| **下一步行动** | 后端补端点                                                                                                              |

---

### OBS-BT-B · `/backtest/runs` 任务历史 KPI「平均耗时」未对接 BE stats

| 字段           | 内容                                                                                              |
| -------------- | ------------------------------------------------------------------------------------------------- |
| **模块**       | 回测 → 任务历史                                                                                   |
| **严重等级**   | P3                                                                                                |
| **根因分类**   | `backend-gap`                                                                                     |
| **发现版本**   | 2026-05-18                                                                                        |
| **现象**       | KPI 卡「平均耗时」显示 `0s`；卡片标注「聚合统计待后端 stats」，FE 当前以页内任务列表前端聚合兜底  |
| **期望**       | BE 在 `/api/backtest/runs` 响应或独立 `/api/backtest/runs/stats` 返回完成率 / 平均耗时 / 失败聚类 |
| **状态**       | 🟢 Open — FE 已留兜底文案                                                                         |
| **下一步行动** | 后端补 stats；FE 接通后回归任务历史 KPI 区                                                        |

---

### BACK-004 · 组合交易日志接口 500（YYYYMMDD 日期解析失败）

| 字段           | 内容                                                                                                                                                                                    |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 我的组合 → 组合详情 → 交易日志 Tab                                                                                                                                                      |
| **严重等级**   | P1                                                                                                                                                                                      |
| **根因分类**   | `backend-bug`                                                                                                                                                                           |
| **发现版本**   | 2026-05-20（组合 Round 1）                                                                                                                                                              |
| **复现步骤**   | 进入组合详情 → 交易日志 Tab（自动加载）                                                                                                                                                 |
| **实际表现**   | `POST /api/portfolio/trade-log` 和 `POST /api/portfolio/trade-log/summary` 均返回 500；前端显示「加载交易日志失败」                                                                     |
| **期望表现**   | 正常返回分页交易记录                                                                                                                                                                    |
| **根因分析**   | 后端 `portfolio-trade-log.service.ts:55` 执行 `new Date("20260220")` 解析 YYYYMMDD 字符串，Node.js 中该格式无效（`new Date("Invalid Date")`），需改用 `new Date("2026-02-20")` 或 dayjs |
| **状态**       | 🔴 Open — 阻断交易日志功能                                                                                                                                                              |
| **下一步行动** | 后端 `trade-log.service` 中对 `startDate`/`endDate` 参数做日期格式转换：`YYYYMMDD → YYYY-MM-DD`，再传 `new Date()`                                                                      |

---

### OBS-SD-A · 股票详情 overview/today-flow 请求体字段与设计文档不一致

| 字段           | 内容                                                                                                                                                          |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 股票详情 → `/stock/detail?code=688525.SH`                                                                                                                     |
| **严重等级**   | P3                                                                                                                                                            |
| **根因分类**   | `design-gap`                                                                                                                                                  |
| **发现版本**   | 2026-05-20（688525.SH Round 1）                                                                                                                               |
| **复现步骤**   | 打开股票详情页，观察 `/api/stock/detail/overview` 和 `/api/stock/detail/today-flow` 请求体                                                                    |
| **实际表现**   | 两个接口请求体使用 `{ code: "688525.SH" }`；前端 `src/api/__tests__/stock.test.ts` 与后端 `StockDetailDto` 也明确以 `code` 为契约                             |
| **期望表现**   | `docs/design/股票详情-重构v3-前端设计.md` N-P2-1 / 接口约定要求 stock-detail 端点 Body 统一使用 `tsCode`                                                      |
| **根因分析**   | 设计文档的统一字段约定与当前后端 DTO / 前端 API 单测存在历史契约差异；直接改前端会导致现有后端接口收不到股票代码                                              |
| **状态**       | 🟢 Open — 不影响页面当前功能，但需要产品 / 后端确认是否统一契约                                                                                               |
| **下一步行动** | 若确认统一为 `tsCode`，需先改后端 `StockDetailDto` 兼容 `tsCode`，再调整前端 `stockDetailApi.overview/todayFlow` 和对应单测；否则更新设计文档说明兼容字段例外 |

---

### BACK-005 · 股票详情「分红配股」接口 404

| 字段           | 内容                                                                                                          |
| -------------- | ------------------------------------------------------------------------------------------------------------- |
| **模块**       | 股票详情 → 公司与股本 → 分红融资 Tab                                                                          |
| **严重等级**   | P1                                                                                                            |
| **根因分类**   | `backend-bug`                                                                                                 |
| **发现版本**   | 2026-05-23（股票详情 Round 3）                                                                                |
| **复现步骤**   | 股票详情 → 点击「公司与股本」→ 点击「分红融资」子 Tab                                                         |
| **实际表现**   | `POST /api/stock/detail/dividend-financing` 返回 404，页面显示原始报文 `Cannot POST ...`                      |
| **期望表现**   | 返回该股票历史分红、配股、融资数据列表                                                                        |
| **根因分析**   | 后端端点未实现。尝试替代路径 `/api/stock/detail/dividends` 和 `/api/stock/dividends` 均 404                   |
| **状态**       | 🔴 Open — 后端接口缺失，分红融资 Tab 完全无数据                                                               |
| **下一步行动** | 后端实现 `POST /api/stock/detail/dividend-financing` 端点；前端当前降级显示原始错误消息，建议同时改为友好提示 |

---

### BACK-006 · 股票详情「财务报表」接口返回重复记录

| 字段           | 内容                                                                                                                                                                                     |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 股票详情 → 财务 → 利润表 / 现金流量表                                                                                                                                                    |
| **严重等级**   | P2                                                                                                                                                                                       |
| **根因分类**   | `backend-bug`                                                                                                                                                                            |
| **发现版本**   | 2026-05-23（股票详情 Round 3）                                                                                                                                                           |
| **复现步骤**   | 股票详情（688139.SH）→ 财务 → 利润表 / 现金流量表                                                                                                                                        |
| **实际表现**   | `POST /api/stock/detail/financial-statements` 返回：利润表 `2026-03-31`×2、`2025-06-30`×2（完全相同的重复行）；现金流量表全部 4 个报告期各重复 1 次（8 行实为 4 期×2）；资产负债表无重复 |
| **期望表现**   | 每个报告期只有一条记录，利润表/现金流量表不应有重复列                                                                                                                                    |
| **根因分析**   | 推测后端 SQL 查询中存在多表 JOIN 导致笛卡尔积，或某个聚合统计表与明细表双重关联。资产负债表无重复说明三表查询逻辑有差异                                                                  |
| **状态**       | 🟡 Open — 前端无法自行修复，后端需排查 SQL/ORM 查询                                                                                                                                      |
| **下一步行动** | 后端排查 `financial-statements` service 的 income/cashflow 分支查询，确认是否有 JOIN 引发重复行；在业务层或查询层对 (tsCode, reportPeriod, statementType) 做 DISTINCT / 去重             |

---

### BUG-ES-03 · 事件分析「分段对比」接口 404

| 字段           | 内容                                                                                                       |
| -------------- | ---------------------------------------------------------------------------------------------------------- |
| **模块**       | 事件研究 → 事件分析 → 分段对比 Tab                                                                         |
| **严重等级**   | P1                                                                                                         |
| **根因分类**   | `backend-bug`                                                                                              |
| **发现版本**   | 2026-05-23（事件研究 Round 1）                                                                             |
| **复现步骤**   | 事件分析 → 选事件类型 → 点开始分析 → 切换「分段对比」子 Tab                                                |
| **实际表现**   | `POST /api/event-study/analyze/by-segment` 返回 404，Alert 显示原始报文                                    |
| **期望表现**   | 返回按行业 / 市值档位分组的超额收益对比数据                                                                |
| **根因分析**   | 后端 `/api/event-study/analyze/by-segment` 端点未实现                                                      |
| **状态**       | 🔴 Open — 后端接口缺失，分段对比功能完全无法使用                                                           |
| **下一步行动** | 后端实现 `POST /api/event-study/analyze/by-segment` 端点；FE 当前 Alert 展示原始错误，建议同时改为友好提示 |

---


### BACK-AL-01 · limit_type 筛选后端未过滤

| 字段           | 内容                                                                                     |
| -------------- | ---------------------------------------------------------------------------------------- |
| **模块**       | 预警监控 → 涨跌停明细 → 筛选栏「涨停 / 跌停」切换                                        |
| **严重等级**   | P1                                                                                       |
| **根因分类**   | `backend-bug`                                                                            |
| **发现版本**   | 2026-05-23（涨跌停明细 Round 1）                                                         |
| **复现步骤**   | `/alert/limit-list` → 点击「跌停」按钮（URL 变 `?lt=DOWN`） → 等待 2+ 秒                 |
| **实际表现**   | 表格仍显示全部 50 条（47 涨停 + 3 跌停），未按 `limit_type: "DOWN"` 过滤                 |
| **根因分析**   | 后端 `POST /api/alert/limit-list` 忽略请求体中的 `limit_type` 参数，始终返回所有类型数据 |
| **状态**       | 🔴 Open — 后端过滤逻辑缺失                                                               |
| **下一步行动** | 后端 `limit-list` 查询中增加 `WHERE limit_type = :limit_type` 条件（当参数非 null 时）   |

---

### BACK-AL-02 · sealAmount 字段未返回

| 字段           | 内容                                                                                 |
| -------------- | ------------------------------------------------------------------------------------ |
| **模块**       | 预警监控 → 涨跌停明细 → 主表「封单额(万)」列 + 股票抽屉                              |
| **严重等级**   | P2                                                                                   |
| **根因分类**   | `backend-bug`                                                                        |
| **发现版本**   | 2026-05-23（涨跌停明细 Round 1）                                                     |
| **复现步骤**   | 打开 `/alert/limit-list` → 查看「封单额(万)」列的数字部分，或打开抽屉查看「封单额」  |
| **实际表现**   | 封单额数字为空，只显示「占流通 X%」副标题；抽屉显示「封单额 万」（数字缺失）         |
| **根因分析**   | 后端 `sealAmount` 字段返回 `null`，`fNumber(Math.round(null))` 渲染为空字符串        |
| **状态**       | 🟡 Open — 后端数据缺失                                                               |
| **下一步行动** | 后端补充 `sealAmount`（封单额，万元）字段；`sealCount`（封板次数）同样缺失，一并补充 |

---

### BACK-AL-03 · streakStatus 返回中文字符串而非英文枚举值

| 字段           | 内容                                                                                                                  |
| -------------- | --------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 预警监控 → 涨跌停明细 → 连板状态列 + 连板梯队                                                                         |
| **严重等级**   | P2                                                                                                                    |
| **根因分类**   | `backend-bug`                                                                                                         |
| **发现版本**   | 2026-05-23（涨跌停明细 Round 1）                                                                                      |
| **复现步骤**   | 通过 React Fiber 读取 `items[0].streakStatus`                                                                         |
| **实际表现**   | 后端返回 `"2连板"` / `"1连板"` / `"1连跌停"` 等中文字符串                                                             |
| **期望**       | 返回英文枚举值 `FIRST_LIMIT / CONSECUTIVE / PROMOTE / BREAK / FLUSH`（对应 FE `LimitStreakStatus` 类型）              |
| **根因分析**   | 后端 `streakStatus` 序列化为中文描述而非枚举 key，导致前端 `STREAK_STATUS_LABEL` / `STREAK_STATUS_COLOR` 查找全部失败 |
| **状态**       | 🔴 Open — FE 已通过 BUG-AL-01 修复加防御性兜底（未知值不显示标签），但连板状态标签功能整体失效                        |
| **下一步行动** | 后端统一返回英文枚举值；FE 修复后连板状态 Chip 可正常显示「首板/续板/晋级/断板/炸板」等标签                           |

---

### BACK-FAS-01 · 选股摘要缺少 matchedRate / missingRate / executionMs 字段

| 字段           | 内容                                                                                                                                                                                      |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 因子选股 `/factor/screening` → KPI 摘要条                                                                                                                                                 |
| **严重等级**   | P1                                                                                                                                                                                        |
| **根因分类**   | `backend-bug`                                                                                                                                                                             |
| **发现版本**   | 2026-05-30（因子选股 Round 1）                                                                                                                                                            |
| **复现步骤**   | 添加 pe_ttm > 30 条件 → 运行选股 → 观察 KPI 摘要条                                                                                                                                        |
| **实际表现**   | `summary` 仅返回 `{universeSize, matchedCount, returnedCount, pageCount}`；`命中率 / 缺失率 / 耗时` 全部显示"—"                                                                           |
| **期望**       | 前端类型 `FactorScreeningSummary` 定义的 `matchedRate / missingRate / asOfTradeDate / executionMs` 均有值                                                                                 |
| **根因分析**   | 后端 `/api/factor/screening` 的 `summary` 字段目前只返回 4 个基础字段，缺少命中率、缺失率、快照日期、耗时等统计信息；另外字段名 `universeSize` ≠ 前端 `universeCount`（已前端侧兜底修复） |
| **状态**       | 🟡 Open — 前端已对 `universeSize` 做了 fallback（BUG-FAS-002 已修复），但其余字段依赖后端补全                                                                                             |
| **下一步行动** | 后端在 screening summary 中补充 `matchedRate / missingRate / asOfTradeDate / executionMs`；字段名统一使用 `universeCount`（或前端双读，已完成）                                           |

---

### BACK-FAS-02 · conditionPassCounts 缺少 beforeCount / missingCount / afterCount

| 字段           | 内容                                                                                                                                                                                    |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **模块**       | 因子选股 → 条件命中漏斗                                                                                                                                                                 |
| **严重等级**   | P1                                                                                                                                                                                      |
| **根因分类**   | `backend-bug`                                                                                                                                                                           |
| **发现版本**   | 2026-05-30（因子选股 Round 1）                                                                                                                                                          |
| **复现步骤**   | 添加任意因子条件 → 运行选股 → 观察条件命中漏斗                                                                                                                                          |
| **实际表现**   | 每条 `conditionPassCounts[i]` 仅含 `{factorName, operator, passCount, remainingCount}`；`beforeCount / missingCount / afterCount / threshold` 均缺失，漏斗显示"— → N（剔除 —，缺失 —）" |
| **期望**       | 前端类型 `FactorScreeningConditionPassCount` 所有字段均有值，漏斗能展示完整的逐步缩漏斗形态                                                                                             |
| **根因分析**   | 对应设计文档 BE-3（conditionPassCounts 详细统计），该功能尚未在后端实现。前端已通过 BUG-FAS-001 修复 NaN 显示，改为 "—" 占位                                                            |
| **状态**       | 🟡 Open — 阻塞于后端 BE-3                                                                                                                                                               |
| **下一步行动** | 后端在 `/api/factor/screening` 的 `conditionPassCounts` 中补充 `beforeCount / missingCount / afterCount / threshold` 字段                                                               |

---

### AI-RT-01 · 运行时能力目录遗漏已发布的资讯与网页工具

| 字段 | 内容 |
| --- | --- |
| **模块** | AI 研究工作台 → 工具规划 / 外部资讯 |
| **严重等级** | P1 |
| **根因分类** | `backend-bug` |
| **发现版本** | 2026-08-07（真实浏览器工具覆盖 Round 4） |
| **复现步骤** | 在 `/agent` 新会话明确要求分别调用 `get_market_news`、`search_web`、`fetch_web_page`，并等待终态。 |
| **实际表现** | Round 5 已将 `get_market_news` 与 `get_convertible_bond_market` 注入运行时目录并完成真实调用；`search_web` / `fetch_web_page` 仍因 `AGENT_SEARCH_PROVIDER=disabled` 且 URL token secret 未配置而保持禁用。 |
| **期望表现** | 已配置安全 provider 时，运行时提供与发布白名单一致的网页工具 schema；未配置时明确维持禁用。 |
| **状态** | 🟡 Partial — 本地资讯与可转债已修复；网页工具等待安全 provider 配置。 |
| **下一步行动** | 配置并验证外部搜索 provider 与 URL token secret 后，再启用网页工具并补启动期一致性校验 / E2E。 |

---

### AI-RT-07 · 组合高级分析依赖的私有数据尚未就绪

| 字段 | 内容 |
| --- | --- |
| **模块** | AI 研究工作台 → 组合高级分析 |
| **严重等级** | P2 |
| **根因分类** | `data-missing` |
| **发现版本** | 2026-08-07（全量工具回归 Round 7） |
| **复现步骤** | 对已存在的测试组合请求仅调用 `get_portfolio_analytics`。 |
| **实际表现** | 基础风险工具可返回 1 个持仓和 HHI，`get_portfolio_analytics` 终态提示“Tool 数据尚未就绪”。 |
| **期望表现** | 组合净值、归因和高级分析所需快照就绪后返回结果；未就绪时给出明确的覆盖时间与补数建议。 |
| **状态** | 🟡 Open |
| **下一步行动** | 回填该测试组合的净值/归因源数据，并在工具错误详情中显示缺失数据集与可用起始日期。 |

---

### QA-UI-01 · Header 图标按钮缺少可见键盘焦点

| 字段 | 内容 |
| --- | --- |
| **模块** | 全局 Header / Accessibility |
| **严重等级** | P1 |
| **根因分类** | `code-bug` |
| **发现版本** | 2026-08-08（六页面 UI 重构集成 Round 1） |
| **复现步骤** | 首页用键盘 Tab 从主题切换移动到通知按钮，检查焦点样式。 |
| **实际表现** | 主题已配置 `MuiButtonBase` 的 `Mui-focusVisible/:focus-visible` 2px 主色焦点环，并有主题回归测试；本轮因登录验证码未获授权，尚未在真实认证 Header 中复验计算样式。 |
| **期望表现** | 所有 Header 图标按钮在 `:focus-visible` 下显示满足对比度的焦点环。 |
| **状态** | 🟡 Verify — 代码与自动化已就绪，等待真实认证浏览器键盘验收。 |
| **下一步行动** | 获得本地登录与验证码处理授权后，在亮/暗主题下用 Tab 复验通知、主题和账户按钮焦点环。 |
